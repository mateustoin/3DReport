# Registro de decisões

Decisões aprovadas pelo responsável pelo projeto. Novas decisões são adicionadas
ao final, com data.

| # | Data | Decisão | Status |
|---|---|---|---|
| 1 | 2026-09-15 | Plataforma inicial: **desktop (JVM)**. Android planejado para depois, sem configuração agora | Aprovada |
| 2 | 2026-09-15 | Módulos: **`core`** (KMP puro, domínio + cálculo) e **`composeApp`** (UI Compose Multiplatform) | Aprovada |
| 3 | 2026-09-15 | Licença **proprietária** (todos os direitos reservados), com Edição Gratuita executável limitada ao orçamento básico | Substituída pela 15 |
| 4 | 2026-09-15 | Primeira entrega: base do projeto + motor de cálculo testado + tela provisória + documentação | Aprovada |
| 5 | 2026-09-15 | Energia: `horas · kW · preço_kWh` (planilha usa kWh = 1,23) | Aprovada |
| 6 | 2026-09-15 | Manutenção: substituir "depreciação %/h" por **custo fixo em R$/hora** | Aprovada |
| 7 | 2026-09-15 | Taxas de marketplace (Shopee) e custo de spray: **fora da edição gratuita** | Substituída pela 15 |
| 8 | 2026-09-15 | Falhas e acabamento: percentuais **sobre o custo de material** | Substituída pela 76 |
| 9 | 2026-09-15 | UI/UX da tela de orçamento: **2 telas** (Orçamento + Configurações) com navegação por abas; Orçamento só pede o que muda por peça (filamento, comprimento, tempo), Configurações reúne os parâmetros da operação (energia, manutenção, falhas, acabamento, máquina, margem) | Substituída pela 13 |
| 10 | 2026-09-15 | Padrão de apresentação: **MVVM** (ViewModel + StateFlow), um ViewModel por tela | Aprovada |
| 11 | 2026-09-15 | Persistência de filamentos e configurações: **em memória por enquanto** (repositórios com valores padrão pré-carregados); armazenamento real fica para decisão futura | Substituída pela 14 |
| 12 | 2026-09-15 | Impressora vira entidade própria, **`PrinterProfile`** (consumo, manutenção, investimento da máquina), salva em catálogo e escolhida por orçamento — uma pessoa pode ter várias impressoras. `PricingSettings` fica restrito aos parâmetros gerais do negócio, iguais para qualquer impressora (energia, falhas, acabamento, administrativo, margem) | Aprovada |
| 13 | 2026-09-15 | Navegação ampliada para **4 abas**: Orçamento, Filamentos, Impressoras, Configurações. Orçamento passa a ter 2 dropdowns (Filamento e Impressora); Filamentos e Impressoras têm tela própria de cadastro (listar/adicionar/editar/excluir) | Aprovada |
| 14 | 2026-09-15 | Persistência real de filamentos, impressoras e configurações: **arquivo JSON local** (`kotlinx.serialization`, em `~/.3dreport/`), por trás da mesma interface de repositório (`expect`/`actual`) | Aprovada |
| 15 | 2026-09-15 | Projeto vira **gratuito e de código aberto**: sem edição paga, sem assinatura, sem verificação de licença no software. Licença de código: **Apache 2.0** (substitui a licença proprietária da decisão 3). Sustentação financeira via doação voluntária (Buy Me a Coffee: `mateustoin`, link no README/`.github/FUNDING.yml`). O repositório GitHub, hoje privado, será aberto para o público pelo responsável do projeto quando ele decidir (não pelo Claude) | Aprovada |
| 16 | 2026-09-15 | Convenções da base **ratificadas como estão** (revisão pedida após o projeto virar open source): pacote `com.threedreport`; identificadores em inglês com KDoc/comentários em português (reavaliar se aparecerem colaboradores que não leem português); valores monetários em `Double`, arredondados só na exibição (reavaliar só se algum bug de arredondamento aparecer na prática) | Aprovada |
| 17 | 2026-09-15 | Priorização do roadmap: **funcionalidades de produto/UI-UX primeiro**, depois instaladores desktop, depois infraestrutura/qualidade (adiada — repositório ainda privado), **Android por último** (só quando o projeto estiver consolidado e houver demanda, sem previsão). Detalhes de "Salvar um orçamento" (nome opcional com padrão automático; foto opcional, no PDF e no histórico, não no copiar/colar; link do modelo opcional, uso só interno, nunca exportado) | Aprovada |
| 18 | 2026-09-15 | Salvar orçamento: formulário **direto na tela de Orçamento** (sem diálogo separado, mesmo padrão dos formulários de Filamentos/Impressoras). Primeira leva de implementação cobre **salvar + histórico** (listar, ver, baixar foto, excluir); exportar em PDF/copiar-colar fica pra uma implementação seguinte | Aprovada |
| 19 | 2026-09-15 | Exportação (PDF/copiar-colar), por ser **documento pro cliente**: mostra só nome + **valor de venda** + foto (PDF) — produção e lucro (uso interno) **nunca aparecem**, junto com o link do modelo (já não aparecia). Exportação fica disponível **só na aba Histórico**, sobre um orçamento já salvo (não direto na tela de Orçamento). Marca d'água personalizada e exportar vários orçamentos num PDF só ficam no roadmap, não entraram nesta leva | Aprovada |
| 20 | 2026-09-15 | Marca d'água no PDF: **só texto** por ora (upload de imagem/logo fica pro roadmap se fizer falta depois), configurada em Configurações. Visual fixo — diagonal, cinza claro translúcido, centralizada — **sem** controles de posição/opacidade pelo usuário nesta primeira versão | Aprovada |
| 21 | 2026-09-15 | Correção: marca d'água ficava **encoberta pela foto** (desenhada antes dela). Passa a ser desenhada **por cima de todo o conteúdo**, inclusive a foto — técnica padrão de marca d'água em documentos. Adicionado também um **rodapé profissional** (linha fina + nome da marca centralizado, no fim da página) quando há marca d'água configurada, pra reforçar a identificação mesmo se a diagonal passar despercebida | Aprovada |
| 22 | 2026-09-15 | Marca d'água diagonal e rodapé viram **opções independentes** (checkbox pra cada uma em Configurações); com um nome preenchido, salvar exige ao menos uma marcada. "Exportar PDF" passa a abrir o diálogo de salvar direto na pasta Documents/Documentos do usuário, em vez do padrão do sistema | Aprovada |
| 23 | 2026-09-15 | Bug de terceiros contornado: arrastar a janela entre monitores com DPI/escala diferentes deixava o conteúdo com o layout antigo até um redimensionamento manual — bug conhecido do Compose Desktop/Skiko (JetBrains/compose-multiplatform#3685 e relacionadas), sem correção oficial. Contornado detectando a troca de monitor e forçando um redimensionamento programático (`Main.kt`) | Aprovada |
| 24 | 2026-09-15 | Exportar vários orçamentos num PDF só: seleção múltipla direto na lista da aba Histórico (checkbox por linha), sem tela separada. Um orçamento por página, na ordem da lista; marca d'água/rodapé (se configurados) em todas as páginas | Aprovada |
| 25 | 2026-09-15 | Serviços opcionais no orçamento (aba "Serviços", catálogo nome+preço; checkboxes na tela de Orçamento). O valor dos serviços selecionados soma só no **Total (venda + serviços)** — o **Lucro exibido não muda**, porque o app não modela o custo de um serviço (só o preço cobrado do cliente), então tratar tudo como lucro puro seria enganoso | Aprovada |
| 26 | 2026-09-15 | Taxa de marketplace: **percentual configurado em Configurações**, marcado por orçamento (checkbox na aba Orçamento). Diferente de um serviço — o marketplace desconta a taxa da venda em vez de somar no total do cliente — então o **valor de venda é ajustado** (`salePrice = baseSalePrice / (1 − taxa)`) pra manter a margem de lucro real igual à de uma venda sem marketplace. Custo de embalagem/spray **entra na aba Serviços** (valor fixo cobrado do cliente), não precisou de mecanismo novo | Aprovada |
| 27 | 2026-09-15 | Versionamento do app: **SemVer**, começando em `0.2.0` pra esta leva (`0.1.0` já usado retroativamente pro trabalho anterior). Bump de **MINOR a cada leva de funcionalidades entregue**; fonte única em `gradle.properties` (`appVersion`, usado no instalador) + `AppVersion.kt` (constante `APP_VERSION`, exibida no rodapé) — mantidos manualmente em sincronia, sem geração automática | Substituída pela 53 |
| 28 | 2026-09-15 | Rodapé fixo em todas as telas: versão do app, nome do autor, link do GitHub, link de doação (Buy Me a Coffee) e um botão "Ajuda" (abre diálogo com versão, descrição curta e resumo de cada aba) | Aprovada |
| 29 | 2026-09-15 | Link do modelo (aba Orçamento e Histórico) vira **hyperlink clicável**, abrindo no navegador padrão do sistema | Aprovada |
| 30 | 2026-09-15 | Confirmação antes de excluir: diálogo (`ui/components/ConfirmDialog`) nomeando o item, em **4 telas** — Filamentos, Impressoras, Serviços (catálogos) e Histórico (orçamento salvo, junto com a foto) — todas com o mesmo risco de exclusão acidental de algo configurado com calma. Edição não ganhou confirmação própria: já exige clicar em "Salvar" pra valer, o que já funciona como confirmação | Aprovada |
| 31 | 2026-09-15 | Processo: toda leva de funcionalidades passa a exigir **bump de versão + entrada no `CHANGELOG.md`** (formato [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/)), no mesmo commit da documentação — não é opcional. Serve de rascunho pronto pras release notes de cada versão quando um release for publicado no GitHub | Aprovada |
| 32 | 2026-09-15 | Peso da peça (`Quote.filamentWeightGrams`, já calculado internamente a partir da densidade do filamento e do comprimento) passa a aparecer no Histórico — só uso interno do criador, junto com produção/venda/lucro; não entra no PDF nem no copiar/colar (mesmo tratamento do link do modelo e dos valores internos) | Aprovada |
| 33 | 2026-09-15 | Distribuição dos instaladores: **GitHub Actions** (`.github/workflows/release.yml`), disparado só por **push de tag** `vX.Y.Z` — nunca automaticamente; sempre confirmar com o responsável antes de criar/empurrar a tag. Builda `.deb`/`.msi`/`.dmg` em 3 runners (um por SO, já que `packageDistributionForCurrentOS` não faz cross-compilation) e junta num **GitHub Release em rascunho** (nunca publicado automaticamente), com a descrição preenchida a partir do `CHANGELOG.md`. Sem assinatura de código por ora (custo recorrente incompatível com o modelo de doação, decisão 15) — usuário precisa ignorar o aviso do SmartScreen/Gatekeeper na primeira execução. Escopo restrito a build de release sob demanda; não reabre a decisão 17 (CI completo de build+teste a cada push segue adiado enquanto o repositório é privado) | Aprovada |
| 34 | 2026-09-15 | Ao tentar cortar o primeiro release (`v0.3.0`), o bundler do macOS rejeitou o build: jpackage exige que o primeiro número do `app-version` seja **≥ 1** (regra do próprio Apple/jpackage, não contorná­vel com `0.x`). Em vez de uma gambiarra só pro macOS (versão de exibição diferente da versão real do pacote), a versão do app **vira `1.0.0`** — condizente com ser o primeiro release de fato publicado. `0.1.0`/`0.2.0` continuam no `CHANGELOG.md` como histórico do desenvolvimento antes do primeiro release; nenhuma versão `0.x` chegou a ser publicada como release | Aprovada |
| 35 | 2026-09-16 | Tema claro/escuro: preferência persistida como `ThemeMode` (`LIGHT`/`DARK`/`SYSTEM`, padrão `SYSTEM`) no mesmo padrão de repositório JSON de `BrandingSettings`/`PricingSettings` (`ThemeRepository`, `~/.3dreport/theme.json`). Troca aplica **na hora** (sem botão "Salvar", diferente de Configurações/Marca d'água) — trocar de tema é uma ação instantânea, não um formulário. Paleta customizada (azul petróleo primário, laranja âmbar secundário) substitui o roxo padrão do Material3, com controle em Configurações (seção "Aparência", segmented buttons) | Aprovada |
| 36 | 2026-09-16 | Modernização de UI, escopo desta leva: paleta de cores customizada (ver decisão 35) + componente `EmptyState` reutilizável aplicado às listas de Filamentos/Impressoras/Serviços (hoje não mostravam nada quando vazias). Fora do escopo, registrado como observação técnica: unificar Filamentos/Impressoras/Serviços num componente genérico de lista/formulário (duplicação real, mas refactor grande à parte) e migrar `App.kt` pra `Scaffold`/`SnackbarHost` no lugar do feedback inline em `Text` vermelho/verde | Aprovada |
| 37 | 2026-09-16 | Cliente vinculado ao orçamento: campo **embutido opcional** em `SavedQuote` (`Client(name, contact?)`), no mesmo espírito de `sourceLink` — não um catálogo reaproveitável como `Service`/`Filament`/`Printer`, por não haver ainda pedido de autocomplete ou histórico por cliente. Uso só interno (Histórico), nunca exportado em PDF ou copiar/colar, mesma régua do link do modelo. Status do pedido: novo enum `OrderStatus` (Orçado → Aprovado → Em impressão → Pronto → Entregue, padrão `ORCADO`), editável no Histórico (dropdown por linha), também uso só interno | Aprovada |
| 38 | 2026-09-16 | Filtro de período (Histórico) e recorte de período (Dashboard) usam um **preset de atalhos** (`PeriodPreset`: Tudo/7 dias/30 dias/Este mês) resolvido do lado JVM com `java.time`, em vez de um seletor de intervalo de datas customizado — cobre o caso de uso real com bem menos UI nova; dá pra evoluir pra um date-range picker depois, se fizer falta. Dashboard: nova aba, com agregação (`core/report/QuoteReport`, função pura sem estado, mesmo estilo do `PricingCalculator`) somando venda/lucro e contando o filamento mais usado sobre a lista de orçamentos já recortada por período | Aprovada |
| 39 | 2026-09-16 | Controle de estoque de filamento: **manual e binário**, não dedução automática por consumo — o responsável do projeto rejeitou a dedução automática por ser imprecisa na prática (falhas de impressão, testes e sobras consomem material sem virar um orçamento salvo). `Filament` ganha `brand`/`colorName` (texto livre, opcionais) e `colorHex` (cor visual, opcional) além de `inStock: Boolean` (padrão `true`), alternado manualmente na tela de Filamentos (botão "Marcar esgotado"/"Marcar em estoque" por linha, sem precisar abrir o formulário de edição). Filamento com `inStock = false` **continua no catálogo** (linha acinzentada, `Modifier.alpha`) mas **some da lista de seleção na tela de Orçamento** — evita escolher por engano um filamento esgotado, sem afetar orçamentos já salvos (que guardam o `Filament` embutido, não uma referência) | Aprovada |
| 40 | 2026-09-16 | Refinamento da decisão 39: um filamento (mesma marca/preço/densidade) passa a admitir **várias cores** (`Filament.colorName`/`colorHex`/`inStock` viram uma lista `colors: List<FilamentColor>`, cada uma com seu próprio controle de estoque), evitando cadastro duplicado quando só a cor muda. Tela de Orçamento ganha um **segundo dropdown "Cor"**, visível só quando o filamento escolhido tem mais de uma cor em estoque — a cor não afeta o cálculo (preço/densidade continuam do filamento), só fica registrada em `PrintJob.filamentColor` (uso interno, exibida no Histórico, nunca exportada). Seletor de cor personalizada: **campo de hex com prévia** (`#RRGGBB`) ao lado da paleta de cores prontas, em vez de um seletor visual completo (matiz/saturação) — mais rápido de implementar e cobre o caso de "não tem esse tom nos presets". Sem migração de dados: como nenhuma versão com o formato antigo (decisão 39) chegou a ser publicada em release, o campo antigo é substituído diretamente | Aprovada |
| 41 | 2026-09-17 | Confirmado (sem precisar instalar nada — inspecionando os `.msi` reais de v1.0.0 e v1.3.0 via a API do Windows Installer) que atualizar o app entre versões preserva `~/.3dreport/`: o instalador (jpackage/WiX) nunca referencia a pasta do usuário — tudo fica sob `INSTALLDIR` (`Program Files\3DReport`) — e as versões compartilham o mesmo `UpgradeCode`, com `FindRelatedProducts`/`RemoveExistingProducts` configurados como major upgrade MSI padrão (substitui a versão anterior automaticamente, sem instalação lado a lado nem exigir desinstalar manualmente antes). Resolve o item do roadmap sem necessidade de um teste de instalação real na máquina do responsável do projeto | Aprovada |
| 42 | 2026-09-17 | Ícone do app: criado programaticamente (cubo isométrico "em camadas" + bico de impressão, cores da paleta do app), sem ferramenta de imagem externa — desenhado via Java2D num script único, com os contêineres `.ico`/`.icns` montados byte a byte (formato documentado, sem precisar de `iconutil`/ImageMagick). Atalho de instalação: a ferramenta de empacotamento (jpackage/WiX, via Compose Multiplatform) **não expõe** a opção de caixinha "criar atalho?" durante a instalação (`--win-shortcut-prompt` do jpackage não tem equivalente na DSL do plugin Gradle, confirmado lendo o código-fonte do plugin) — só um `Boolean` fixo (`shortcut = true`/`false`, sempre cria ou nunca cria). Optou-se por **sempre criar** atalho na área de trabalho e no menu Iniciar no Windows (e no Linux), em vez de investir em contornar essa limitação com uma tarefa Gradle customizada chamando o jpackage diretamente | Aprovada |
| 43 | 2026-09-17 | Atalhos de teclado: escopo focado no "fluxo rápido de criar orçamento" pedido no roadmap — navegação entre abas (`Ctrl/Cmd+1..7`), salvar (`Ctrl/Cmd+S`) e limpar (`Ctrl/Cmd+N`) só na aba Orçamento, e `Esc` cancelando formulário de catálogo aberto (Filamentos/Impressoras/Serviços). Implementado com `Modifier.onPreviewKeyEvent` no `Surface` raiz de `App.kt` (intercepta antes do campo focado, funciona em qualquer aba/campo) — não usa o parâmetro `onPreviewKeyEvent` do `Window` (`Main.kt`) por não precisar: o modificador no topo da árvore de composição já cobre o app inteiro. **Fora do escopo, por não ser alcançável sem mexer na arquitetura:** `Esc` não fecha os diálogos de confirmação de exclusão nem o de Ajuda — ambos usam `AlertDialog`, que no Compose Desktop abre como uma janela OS separada (fora do alcance do handler da janela principal), e fechar via `Esc` dependeria do comportamento padrão do próprio `Dialog` do Compose Desktop, não de código deste app | Aprovada |
| 44 | 2026-09-17 | Catálogo/portfólio exportável: reaproveita a seleção múltipla que já existe no Histórico (mesmos checkboxes usados por "Exportar selecionados (PDF)") — nenhuma UI de seleção nova, nem campo novo em `SavedQuote` pra marcar "peça pronta" vs "sob encomenda" (a curadoria é o próprio ato de selecionar). Layout: grade A4, 2 colunas, foto até 180×180pt + nome + venda por célula, paginação automática (4 itens/página no teste real), título "Catálogo de produtos" em toda página — diferente do export "um orçamento por página" que já existia, pensado pra divulgação (mandar pro cliente, postar em grupo de venda) em vez de documento formal. Usa a mesma marca d'água/rodapé configurados (`BrandingSettings` ativo), mesma régua de conteúdo (sem produção/lucro/link interno) | Aprovada |
| 45 | 2026-09-17 | Templates de orçamento: **biblioteca de presets nomeados que só copiam pro `BrandingSettings` ativo** (`TemplateListViewModel.applyToActive`), em vez de substituir `BrandingRepository` por uma lista com "template ativo". Motivo: `BrandingSettings`/marca d'água já foi lançado (decisão 20, presente desde a v1.0.0) — substituir o repositório exigiria migração de dados reais de quem já configurou; a biblioteca aditiva evita isso completamente, e mantém a seção "Marca d'água do PDF" em Configurações como o único editor da config em uso (sem duplicar onde configurar). Sem indicador de "qual template está ativo" na lista — é uma cópia one-shot ("Usar este"), não um vínculo persistente. Nova aba Templates entre Serviços e Configurações; atalho de teclado `Ctrl/Cmd+7`→Templates, `Ctrl/Cmd+8`→Configurações (renumeração da decisão 43, ainda recente) | Aprovada |
| 46 | 2026-09-17 | Múltiplas moedas/localização: 4 moedas (BRL, USD, EUR, GBP — confirmado com o responsável do projeto), cada uma com símbolo **e** convenção própria de separador decimal/milhar (BRL/EUR usam vírgula decimal e ponto de milhar; USD/GBP o inverso) — não é só trocar o símbolo. Escolhida em Configurações → Moeda (`CurrencyRepository`, mesmo padrão do `ThemeRepository`), aplicada em tempo real. Threading: telas usam `@Composable fun Double.toMoney()`, lendo um `CompositionLocal` (`LocalCurrency`) provido uma vez no topo de `App.kt` — nenhuma tela recebe a moeda como parâmetro, só chama `.toMoney()` como já chamava `.toBrl()` antes (renomeado por clareza). PDF e copiar/colar, por rodarem fora de composição, usam uma função pura `toCurrencyText(currency = Currency.BRL)` com parâmetro default, passada explicitamente pelo `QuoteHistoryViewModel` (que ganhou `CurrencyRepository` como dependência). De passagem, a formatação ganhou separador de milhar, que não existia antes ("R$ 1234,50" virava "R$ 1.234,50"). Não traduz a UI (só a moeda) — fora do escopo do pedido do roadmap | Aprovada |
| 47 | 2026-09-17 | Revisão da decisão 45, dias depois, em duas partes: **(1)** a aba Templates **deixa de ter formulário próprio** de criar/editar preset (removido `TemplateFormState`) — os dois formulários paralelos (Templates e Configurações → Marca d'água) editavam os mesmos 3 campos sem se refletir um no outro, o que era ambíguo sobre qual "valia". Configurações passa a ser o **único lugar de edição ao vivo**; ganhou o botão "Salvar como template" (só pede um nome, tira uma foto do que está no formulário na hora do clique — mesmo sem ter clicado em "Salvar" antes, permitindo arquivar variações sem tornar cada uma a config ativa). De quebra, resolve a lacuna que a própria decisão 45 registrava ("sem indicador de template ativo"): como carregar um template agora sempre reflete exatamente os mesmos 3 campos, a lista passa a mostrar um badge "Ativo" no template cujos campos batem com o `BrandingSettings` corrente (`TemplateListViewModel.activeTemplateId`, comparação estrutural simples, sem vínculo persistente/id gravado). **(2)** revisão de usabilidade feita na sequência, ainda no mesmo dia (pedido do responsável do projeto): uma **aba fixa no menu principal era desproporcional** pra uma funcionalidade tão estreita, usada só dentro de uma sub-seção de Configurações (diferente de Filamentos/Impressoras/Serviços, catálogos usados no fluxo principal de Orçamento) — **a aba Templates é removida** (volta a 7 abas, `Ctrl/Cmd+7`→Configurações). A lista de "Carregar"/"Excluir"/badge Ativo passa a abrir como **diálogo modal** (`ui/templates/TemplateListDialog`, mesmo padrão de `ConfirmDialog`), acionado pelo botão "Ver templates salvos" ao lado de "Salvar como template", em Configurações → Marca d'água do PDF | Aprovada |
| 48 | 2026-09-17 | Padronização das notas de release (pedido do responsável do projeto ao cortar a v1.8.0): o `CHANGELOG.md` continua só com o conteúdo técnico (formato Keep a Changelog, decisão 27/31), sem propaganda — quem lê o arquivo no repositório não precisa disso. O texto extra (introdução breve do projeto, CTA de doação, instruções de instalação por SO) foi pra dentro do **workflow** (`.github/workflows/release.yml`, job `draft-release`), que já envolve a seção do `CHANGELOG.md` extraída com esse conteúdo fixo antes de criar o rascunho do GitHub Release — padronizado automaticamente pra **todo** release futuro, sem esforço manual extra a cada tag | Aprovada |
| 49 | 2026-09-17 | Preparativos pra tornar o repositório receptível a público antes de abrir (decisão 15): (1) descrição do repositório e tópicos do GitHub configurados (`gh repo edit`), focados em quem vende impressão 3D; (2) README/site em **só português** (público-alvo é o mercado brasileiro, mesmo idioma da documentação já existente) — sem versão em inglês por ora; (3) escopo adiantado da seção 4 do roadmap (CI, `CONTRIBUTING.md`, templates de issue/PR, badges) — `CODE_OF_CONDUCT.md` avaliado e deixado de fora por ora; (4) site institucional em `site/` (HTML/CSS estático, sem framework), publicado via `.github/workflows/pages.yml` (GitHub Actions → GitHub Pages), landing page + manual rápido em página própria, com SEO (meta tags, JSON-LD, `sitemap.xml`, `robots.txt`) voltado a termos como "orçamento impressão 3D". **Observação técnica:** a configuração de Pages (`Settings → Pages`) já foi criada via API com `build_type=workflow` — isso não publica nada sozinho (só quando o workflow rodar após um push), mas GitHub Pages de repositório privado é servido publicamente na internet mesmo assim (não existe controle de acesso por privacidade do repo fora de plano Enterprise Cloud) — outro motivo pra não fazer push do primeiro deploy antes do responsável decidir tornar o repositório público | Aprovada |
| 50 | 2026-09-17 | Repositório GitHub (`mateustoin/3DReport`) tornado **público** pelo responsável do projeto (decisão 15 cumprida). Destrava, junto com o que já estava pronto na decisão 49: publicação real do site em GitHub Pages (primeiro push do workflow `pages.yml`), links de download/releases funcionando pra quem visita o site, badges/CI visíveis publicamente, e a assinatura "Gerado com 3DReport" nos PDFs podendo linkar pro site/repo (item ainda no backlog, só a dependência de visibilidade foi resolvida) | Aprovada |
| 51 | 2026-09-17 | Reforço de SEO do site (pedido do responsável do projeto): além de "orçamento impressão 3D", o site precisa performar também pra "venda de impressão 3D"/"vender impressão 3D", já que o público-alvo tanto orça quanto vende. Title, meta description, Open Graph/Twitter Card e a descrição `SoftwareApplication` do JSON-LD passaram a citar as duas coisas; nova seção "Do orçamento à venda concluída" (`site/index.html#gestao-de-vendas`) expõe o pipeline de status do pedido (Orçado → Aprovado → Em impressão → Pronto → Entregue), cliente vinculado e dashboard de vendas, funcionalidades que já existiam no app mas não apareciam no site; nova pergunta no FAQ (visível e no `FAQPage` JSON-LD) cobrindo diretamente "gerenciar a venda de impressão 3D como negócio"; manual ganhou um 7º passo ("Acompanhe a venda até a entrega") e título/JSON-LD `HowTo` atualizados. Tópicos `venda-impressao-3d`/`vendas` adicionados ao repositório GitHub pelo mesmo motivo. De passagem, corrigido um bug visual encontrado durante a revisão: `.formula-box` não preservava quebra de linha (faltava `white-space: pre-wrap`) | Aprovada |

| 52 | 2026-09-17 | Screenshots reais do site (o responsável do projeto adicionou `orcamento.png`, `historico.png` e `pdf.png`, 1280×800, em `site/assets/screenshots/`, com os nomes já previstos em `PLACEHOLDER.md`, que foi removido). Reaproveitadas no README também. `og:image`/`twitter:image` de `index.html`/`manual.html` (antes o ícone do app) passaram a usar `orcamento.png`, com `twitter:card` mudado pra `summary_large_image` pra mostrar a prévia em tamanho grande | Aprovada |
| 53 | 2026-09-18 | Revisão da decisão 27 (pedido do responsável do projeto): bump de **MINOR a cada leva** nunca deixava espaço pra `PATCH`, mesmo quando a leva era só uma correção pequena ou uma atualização de documentação — de `0.1.0` até `1.8.0`, todo bump real foi MINOR (`1.0.0` foi exceção forçada pelo bundler do macOS, decisão 34), nunca PATCH, apesar do próprio `development.md` já descrever PATCH como "correções isoladas fora de uma leva". Regra reforçada, alinhada a convenção usada em projetos profissionais (compatível com [Conventional Commits](https://www.conventionalcommits.org/pt-br/)): **PATCH** (`z`) para correção de bug isolada ou leva que só atualiza documentação, sem funcionalidade nova nem mudança de comportamento visível; **MINOR** (`y`) para leva que entrega funcionalidade nova ou melhoria visível ao usuário (mesmo critério de antes); **MAJOR** (`x`) continua reservado pra quebra de compatibilidade, sem caso de uso previsto. Critério de desempate: se uma leva mistura correção(ões) com funcionalidade nova, o bump segue sendo MINOR (a funcionalidade nova já exige isso) — PATCH é só pra levas que **não** têm nada de funcionalidade nova. Não muda onde a versão mora (`gradle.properties`/`AppVersion.kt`) nem o processo de "sempre bumpar, mesmo leva pequena" (decisão 31) | Aprovada |
| 54 | 2026-09-19 | Presets de cadastro (impressoras e filamentos), pedido do responsável do projeto (2026-09-18), implementados como dado embutido no app (arquivo Kotlin, sem rede/download nem JSON externo) — mesmo espírito de "mantido manualmente em sincronia" já usado pra versão do app. **Impressoras**: `PrinterProfile` não mudou — um preset só preenche `name`/`printerPowerWatts` no formulário de nova impressora (diálogo `PrinterPresetDialog`, botão "Escolher da lista"); manutenção/investimento continuam manuais. Duas iterações no mesmo dia: a primeira usava uma estimativa de **consumo médio durante a impressão** (só 5 modelos, porque a maioria das marcas não publica esse número, só o nominal da fonte/PSU); o responsável do projeto pediu pra trocar de critério depois de testar — cobrir **todas as principais marcas** (Bambu Lab, Creality, Prusa, Elegoo, Anycubic, Flashforge, Snapmaker) usando a **potência máxima/nominal do manual ou ficha técnica oficial** em vez da média (mais fácil de sourciar de forma confiável, mesmo que superestime o custo de energia real — por isso o diálogo deixa isso explícito no texto, pra quem escolher saber que vale ajustar pra baixo). Catálogo final: 39 modelos; ficaram de fora modelos sem número oficial confiável (Anycubic Vyper, Flashforge Creator Pro/Guider II, Anycubic Kobra original, CR-10 original) — não inventado. Quando o fabricante publica valores diferentes por tensão (ex.: Bambu A1: 1300 W@220V vs 350 W@110V), usa-se o maior, coerente com "potência máxima". Diálogo ganhou um campo de busca (por marca ou modelo, mesmo estilo `contains`/case-insensitive do filtro do Histórico), necessário com o catálogo maior. **Filamentos**: `Filament` ganhou `materialType: String?` (texto livre, mesmo tratamento de `brand`) — tela de Filamentos ganhou um chip de "Tipo de material" (PLA, PETG, ABS, ASA, TPU, Nylon, PC, HIPS, PVA, PLA-CF, PETG-CF, Madeira, Personalizado) que auto-preenche a densidade pros tipos com valor confiável e deixa em branco pros compósitos (densidade varia demais por fabricante/% de carga) — sempre editável depois. Chips de marca (11 internacionais + 5 brasileiras) abaixo do campo Marca, mesmo princípio (sugestão, nunca obrigatório). **Escolhi reusar o padrão de chip de texto já usado nas cores de filamento**, em vez de um dropdown novo, pra manter uma única linguagem de interação no formulário. **Ícones avaliados e descartados por ora**: as 3 telas de catálogo (Filamentos/Impressoras/Serviços) não usam nenhum ícone gráfico hoje — introduzir um só nas duas telas novas criaria inconsistência com Serviços (que já está no backlog técnico pra uma futura unificação das 3 telas, ver "Observações técnicas" no roadmap) e exigiria uma dependência nova (nenhuma lib de ícones no projeto) só por um ganho visual marginal; revisar essa decisão junto da unificação das telas de catálogo, se/quando ela acontecer | Aprovada |

| 55 | 2026-09-20 | Preencher peso de filamento e tempo de impressão a partir do G-code do fatiador, item de maior prioridade do roadmap (levantado em pesquisa de concorrentes, 2026-09-19): implementado como leitura de texto dos comentários de metadados do arquivo (`core/slicer/GCodeMetadataParser`, função pura testada, sem interpretar nenhum comando de movimento) — cobre os formatos do PrusaSlicer/Bambu Studio/OrcaSlicer (`; filament used [mm] = ...`, `; estimated printing time (...) = 1h 23m 45s`) e do Cura (`;Filament used: 2.5m`, `;TIME:12345`), com soma de múltiplos extrusores quando presentes. Botão "Preencher a partir do G-code" na tela de Orçamento (`platform/pickGCodeFile`, mesmo padrão de `pickImageFile`) chama o parser e preenche os campos existentes de comprimento/tempo — que continuam editáveis manualmente depois, e ficam do jeito que estavam se o arquivo não tiver o dado reconhecido (mensagem explicando o que não foi encontrado, sem bloquear o preenchimento manual). Não depende de nenhuma fase do item "Visualização e análise de STL" (que seguem no roadmap, com prioridade reavaliada por causa deste item) | Aprovada |

| 56 | 2026-09-20 | Revisão da decisão 55, no mesmo dia (pedido do responsável do projeto): fatiadores da família PrusaSlicer (PrusaSlicer, SuperSlicer, OrcaSlicer, Bambu Studio) embutem uma prévia renderizada do modelo no próprio G-code (comentários `; thumbnail begin WxH tamanho` / `; thumbnail end`, base64, geralmente em várias resoluções) — o parser (`core/slicer/GCodeMetadataParser`) passou a extrair essa miniatura também, junto com comprimento/tempo, escolhendo a de maior resolução declarada quando há mais de uma. Cura não embute isso em G-code puro (é um formato próprio de outros fatiadores dessa família), por isso fica de fora nesse caso — comportamento esperado, não uma lacuna a preencher. A miniatura só é aplicada como foto do orçamento **se nenhuma foto já tiver sido escolhida** (`SaveQuoteFormState.photo == null`), pra nunca sobrescrever uma foto de produto de verdade que o usuário já tenha selecionado — se já havia uma, a mensagem de importação avisa que a do G-code foi ignorada. Pedido complementar: um botão "Desfazer importação do G-code", que aparece junto da mensagem de resultado da importação, limpando comprimento/tempo (volta a vazio, não ao valor anterior) e removendo a foto **só se ela também tiver vindo do G-code** (`SaveQuoteFormState.photoFromGCode`, não mexe numa foto escolhida manualmente antes ou depois da importação) | Aprovada |

| 57 | 2026-09-20 | Correção (reportada pelo responsável do projeto): o diálogo de "Preencher a partir do G-code" mostrava todos os arquivos no Windows, não só `.gcode`/`.gco`/`.g` — apesar de `FileDialog.filenameFilter` já estar configurado desde a decisão 55. Causa: o peer nativo do `java.awt.FileDialog` no Windows não chama o `FilenameFilter` do Java de volta (limitação antiga e conhecida do AWT nessa plataforma, sem fix da JDK) — o filtro só funciona de fato em plataformas cujo peer honra esse callback (ex. GTK no Linux). Corrigido definindo `FileDialog.file` com o padrão wildcard (`*.gcode;*.gco;*.g`) antes de abrir o diálogo, que o Windows usa como filtro de fato — mantido o `filenameFilter` também, sem custo, pra continuar funcionando nas plataformas que o respeitam. Mesma causa provavelmente afeta o diálogo de escolher foto (`pickImageFile`), não corrigido aqui por não ter sido reportado — mesma correção se aplicaria lá se algum dia incomodar | Aprovada |

| 58 | 2026-09-20 | Correção da correção (decisão 57 não resolveu, reportado pelo responsável do projeto): o wildcard `*.gcode;*.gco;*.g` definido em `FileDialog.file` não virou um filtro de tipo no diálogo do Windows — foi parar na caixa de texto do nome do arquivo, sem filtrar a lista. Diagnóstico revisado: o `java.awt.FileDialog` no Windows moderno não expõe um combo de "tipo de arquivo" filtrável por API Java de forma confiável, nem por `filenameFilter` (decisão 55) nem pelo wildcard em `file` (decisão 57) — é uma limitação do próprio peer nativo, não dá pra contornar mantendo esse componente. Trocado por `javax.swing.JFileChooser` com `FileNameExtensionFilter` (`isAcceptAllFileFilterUsed = false`, sem opção "todos os arquivos") nos dois diálogos de escolher arquivo do app — G-code (`pickGCodeFile`, extensões `gcode`/`gco`/`g`) e foto (`pickImageFile`, extensões `png`/`jpg`/`jpeg`/`gif`/`bmp`/`webp`, pedido explícito de aplicar a mesma correção ali também). `JFileChooser` é um diálogo Swing (não o diálogo nativo do SO) — visual ligeiramente diferente do Explorer do Windows, mas com filtro de tipo que funciona de fato em qualquer plataforma, o que resolve o problema reportado | Aprovada |

| 59 | 2026-09-20 | Revertida a decisão 58 (pedido do responsável do projeto, ao ver o resultado): o `javax.swing.JFileChooser` filtrava de fato, mas o visual Swing destoa do resto do app/SO — preferência explícita por manter o diálogo **nativo** (`java.awt.FileDialog`), mesmo que isso signifique não ter filtro de tipo garantido no Windows (limitação do peer nativo do AWT, ver decisões 57/58). Critério dado pelo responsável do projeto: filtro de arquivo é só uma conveniência de busca, não algo que impede escolher o arquivo certo manualmente — não vale trocar o componente visual nativo por causa disso. `pickGCodeFile`/`pickImageFile` voltam exatamente ao estado da decisão 55 (`FileDialog` + `filenameFilter`, sem o wildcard da decisão 57) | Aprovada |

| 60 | 2026-09-20 | Instrução de instalação do macOS corrigida (testado na prática pelo responsável do projeto num Mac real, ao instalar a v1.11.3): "clique com o botão direito → Abrir" não bastou pra liberar o app no Gatekeeper — precisou ir em **Ajustes do Sistema → Privacidade e Segurança** e clicar em "Abrir mesmo assim" no aviso sobre o app bloqueado. Textos atualizados pra orientar os dois caminhos (clique direito primeiro, com o caminho de Ajustes como alternativa quando não resolver) em `README.md`, `site/index.html`, `docs/development.md` e no template de release notes (`.github/workflows/release.yml`) — esse último já corrige todo release futuro, não só o atual | Aprovada |

| 61 | 2026-09-21 | Fase 1 do roadmap (visualização de STL), duas decisões juntas: **(1)** anexar/guardar o STL no histórico entra **antes e separado** do parser/visualizador (pedido do responsável do projeto) — organização de arquivo de modelo já é valor real por si só (recuperar o STL de uma venda antiga pra vender de novo pra outro cliente), sem depender do visualizador estar pronto. Implementado exatamente como o `photo`/`sourceLink` já funcionam: `platform/pickStlFile` (mesmo padrão `FileDialog` nativo de `pickImageFile`/`pickGCodeFile`), `SavedQuote.stlFileName`, persistido em `~/.3dreport/models/`, com "Baixar STL" no Histórico e exclusão em cascata junto do orçamento — uso só interno, nunca exportado. **Ideia registrada pro futuro** (não implementada agora): também aceitar o `.3mf` do projeto do fatiador, opcional, pra recuperar as configurações de fatiamento usadas (não só a geometria) numa venda repetida da mesma peça. **(2)** Decisão técnica do visualizador 3D (rendering), pendente desde que a Fase 1 foi escrita: entre renderização nativa via OpenGL (LWJGL), WebView embutido + three.js, ou um rasterizador simples desenhado no `Canvas` do Compose — escolhido o **rasterizador simples** (sombreamento plano por triângulo via produto escalar normal·luz, ordenação pintor pra profundidade, câmera orbital por arrasto/scroll). Critério: entrega tudo que foi pedido funcionalmente (orbitar, zoom, mudar ângulo, exportar como foto) sem somar nenhuma dependência nova nem inflar o instalador — WebView+three.js somaria uma engine de navegador embarcada (100+ MB por SO via JCEF, incompatível com o perfil leve do app); OpenGL nativo exigiria misturar um componente pesado do AWT dentro da janela do Compose, combinação com histórico de problemas de sobreposição entre os dois. Trade-off aceito: sombreamento mais simples (sem gradiente suave por pixel) e desempenho pode cair em malhas muito densas — aceitável pro caso de uso (produtos de venda, não scans 3D de altíssimo detalhe); dá pra evoluir pra OpenGL depois se a qualidade virar um problema real na prática | Aprovada |

| 62 | 2026-09-21 | Correções no visualizador 3D (decisão 61) depois de testar com STL reais (feedback do responsável do projeto): **(1)** sentido do arrasto invertido — arrastar pra direita girava a peça pra esquerda; trocado o sinal do delta de yaw pra girar no mesmo sentido do arrasto (mais intuitivo, como segurar o objeto). **(2)** dar zoom com a roda do mouse também rolava a página inteira (o `Canvas` está dentro do `Column` com scroll da tela de Orçamento) — corrigido consumindo o evento de scroll (`PointerInputChange.consume()`) dentro do próprio visualizador, que passa a não vazar mais pro contêiner por trás. **(3)** sem nenhuma borda/fundo diferenciando a área do visualizador do resto da tela — envolvido num `OutlinedCard`. **(4)** STL com muitos triângulos ficava perceptivelmente mais lento de arrastar — dois ajustes de desempenho sem trocar a abordagem (continua sendo o rasterizador em `Canvas` da decisão 61): `StlTriangle.computedNormal`/`centroid` passam a ser calculados **uma vez na criação** da malha em vez de recalculados a cada frame (não dependem da câmera, só da geometria); os objetos `Path` usados pra desenhar cada triângulo passam a ser **reaproveitados entre frames** (um pool do tamanho da malha, resetado e preenchido de novo a cada redesenho) em vez de recriados do zero a cada arrasto — que era o maior custo em malhas densas. Pan e otimizações adicionais (ex.: nível de detalhe pra malhas muito grandes) continuam fora de escopo, registrados no roadmap | Aprovada |

| 63 | 2026-09-21 | Três ajustes no visualizador 3D/tela de Orçamento, depois de mais uma rodada de teste com STL reais (feedback do responsável do projeto): **(1)** um STL muito pesado travava o app inteiro ao tentar carregar/renderizar — corrigido com `core/stl/peekStlTriangleCount` (conta triângulos sem montar a malha — lê 4 bytes no caso binário, conta ocorrências de `endfacet` no ASCII) checado **antes** de chamar o parser completo; acima de 500 mil triângulos (limite heurístico, ajustável), o app não tenta renderizar e avisa o motivo, mas **continua salvando o STL normalmente** pro criador recuperar depois — só a prévia 3D fica de fora. **(2)** "Exportar a visualização como imagem" (item pendente da Fase 1) implementado: `Stl3DViewer` ganhou um `Stl3DViewerState` hoisted (ângulo/zoom vivem fora do Canvas) com `captureSnapshot`, que renderiza a mesma cena off-screen via `CanvasDrawScope` no ângulo atual da tela; `platform/encodeImageBitmapToPng` (Skia, `Image.makeFromBitmap(...).encodeToData(PNG)`) converte pra bytes; botão "Capturar como foto do orçamento" usa o resultado como a foto do orçamento (mesmo campo já existente, sobrescreve sem perguntar — ação explícita do usuário, mesmo espírito de `pickPhoto`). **(3)** o formulário de salvar (nome, foto, STL, link, cliente) só aparecia depois de um cálculo válido, impedindo anexar STL/foto antes de preencher filamento/impressora/comprimento/tempo — não fazia sentido dado que anexar esses arquivos independe do cálculo. O formulário passa a aparecer sempre; só o botão "Salvar orçamento" continua exigindo um cálculo válido (`enabled = canSave`), com um aviso explicando o que falta quando desabilitado | Aprovada |

| 64 | 2026-09-21 | Editar um orçamento já salvo (pedido do responsável do projeto: refazer o orçamento inteiro por um erro de digitação era uma UX ruim). Reabre o `SavedQuote` na própria aba Orçamento — botão "Editar" no Histórico chama `QuoteViewModel.loadForEditing`, que repopula filamento/cor/impressora/comprimento/tempo/serviços/marketplace a partir do retrato salvo (`Quote` ganhou `printerId`/`printerName`, snapshot da impressora usada — não existia antes, sem isso não daria pra reselecionar a impressora certa ao editar) e o formulário de nome/foto/STL/link/cliente recarregando os anexos do disco (`QuoteHistoryRepository.photoBytes`/`stlBytes`). Salvar de novo chama `QuoteHistoryRepository.update` (novo, ao lado de `save`) em vez de criar uma entrada nova: **mantém `id` e `savedAtEpochMillis`** (data de criação original nunca muda, conforme pedido), troca o resto do retrato, e marca `lastEditedEpochMillis` — exibido no Histórico como "· Editado em DD/MM/AAAA HH:mm" ao lado da data de criação, discreto (mesmo estilo de texto secundário), só quando existe. `App.kt` troca pra aba Orçamento automaticamente ao clicar "Editar". Compatibilidade: orçamentos salvos antes desse campo existir simplesmente não têm impressora restaurável (`printerId` nulo) — cai no mesmo fallback que já existia pra "nenhuma impressora selecionada" (usa a primeira do catálogo), sem quebrar nada, só sem restaurar a impressora original nesses casos antigos | Aprovada |

| 65 | 2026-09-21 | Correção do bug de fotos WebP sumindo do PDF exportado (investigado e registrado no roadmap em 2026-09-17, com duas opções em aberto: A — suportar WebP na exportação, B — não aceitar/reconverter no upload). Escolhida a opção **A sem dependência nova**: em vez de adicionar um plugin ImageIO (ex.: `imageio-webp` da TwelveMonkeys), a exportação de PDF (`platform/QuotePdfExporter.jvm.kt`) passa a decodificar a foto com `decodeImageBitmap` (Skia — a mesma função já usada pra miniatura no app) e converter com `ImageBitmap.toAwtImage()` (extensão do Compose Desktop) pro `BufferedImage` que o PDFBox precisa, no lugar de `javax.imageio.ImageIO.read(...)` (que retornava `null` em silêncio pra WebP, sem exceção). Motivo de preferir A sobre B: a correção fica só na **leitura**, então conserta retroativamente fotos WebP **já salvas** antes desse fix, sem precisar de migração — B (reconverter no momento de salvar) só resolveria uploads futuros. Reaproveita a mesma infraestrutura Skia que o app já usa pra outras coisas (miniatura, captura do visualizador 3D — decisão 63), sem somar dependência nova. Teste de regressão codifica um WebP de verdade via Skia (`Image.encodeToData(EncodedImageFormat.WEBP)`) e confere que a imagem foi de fato embutida como XObject no PDF gerado — o teste antigo (`pdfIsGeneratedWithAPhoto`) só conferia a contagem de páginas, que não pegaria essa regressão | Aprovada |

| 66 | 2026-09-21 | Ordem de implementação das fases de STL: **pular a Fase 2** (estimativa de peso/tempo pela geometria) e ir direto pra **Fase 3** (nível de dificuldade), decisão do responsável do projeto diante da ressalva já registrada em 2026-09-19 — a importação de G-code cobre a maior parte do valor da Fase 2, com mais precisão (considera suporte/purga), então investir na estimativa geométrica própria teria pouco retorno adicional; a Fase 3 resolve um problema que nem G-code nem Fase 2 cobrem (dificuldade de impressão, não peso/tempo) e não depende da Fase 2. Fase 3 implementada como planejado no roadmap: `core/stl/StlAnalyzer` calcula área de superfície e volume (método do divergente/soma de tetraedros a partir da origem, `Σ v1·(v2×v3)/6`, válido pra malha fechada com orientação consistente), % de superfície em overhang (ângulo configurável, padrão 45°, comparado via seno pra evitar `acos` por triângulo), componentes desconexos (união-busca sobre triângulos que compartilham vértice, vértices casados por quantização a 0,001mm pra tolerar ruído de ponto flutuante) e checagem de malha manifold (toda aresta não-direcionada deve aparecer em exatamente 2 triângulos). **Ajuste em relação ao texto original do roadmap:** a "razão área ÷ volume" bruta pedida ali cai com o tamanho do objeto pra qualquer forma (confundiria tamanho com complexidade real) — implementado como quociente isoperimétrico (área ÷ área de uma esfera do mesmo volume, sempre ≥ 1, invariante a escala) em vez da razão bruta. Nível de dificuldade (Fácil/Médio/Difícil) combina essa razão + % overhang + contagem de triângulos + componentes desconexos, cada um pontuando 0/1/2, soma decidindo o nível final — limiares são heurística sem benchmark real, ajustáveis. Exibido ao vivo na tela de Orçamento, logo abaixo do visualizador 3D (mesma condição de tamanho de malha já usada pro visualizador — decisão 63); **não persistido** no `SavedQuote`/Histórico por ora, sem pedido concreto pra isso. Testado com um tetraedro construído e verificado à mão (área, volume e manifold conferidos manualmente antes de escrever o teste) — validação forte pra geometria que seria fácil de errar silenciosamente (ex.: orientação de winding invertida numa face só) | Aprovada |

| 67 | 2026-09-21 | Documentação do nível de dificuldade do STL (decisão 66), pedida pelo responsável do projeto depois de testar e não ficar claro como o cálculo funciona: nova seção "Análise de STL (nível de dificuldade)" em [pricing-formulas.md](pricing-formulas.md) (fórmulas completas — área, volume, quociente isoperimétrico, overhang, componentes, manifold, tabela de limiares/pontuação), mesmo padrão da seção "Cálculos" já existente pro motor de precificação. Site institucional (`site/index.html`): novo card em "Funcionalidades", nova pergunta no FAQ visível **e** no JSON-LD `FAQPage` (mantidos sincronizados, mesmo padrão das perguntas já existentes) explicando em linguagem simples o que Fácil/Médio/Difícil significa e que é uma estimativa geométrica, não simulação de fatiador. `site/manual.html`: novo card em "Próximos passos" apontando o recurso pra quem já fez o primeiro orçamento | Aprovada |

| 68 | 2026-09-21 | Fila de impressão / agenda da impressora: colocada na aba **Impressoras** (não no Dashboard), porque é sobre o estado **atual** de ocupação de cada máquina, não uma métrica de período — misturar com o filtro de período do Dashboard (7 dias/30 dias/etc.) seria confuso (um pedido "Em impressão" salvo há 10 dias continua ocupando a impressora agora, independente do filtro). `core/report/PrintQueueReport` soma `quote.job.printTimeMinutes` dos `SavedQuote` com status `EM_IMPRESSAO` cujo `Quote.printerId` bate com cada impressora cadastrada, e mostra o resultado (horas + contagem de pedidos) direto embaixo de cada linha na lista de Impressoras. Dependência: como `Quote.printerId` só existe desde a decisão 64 (feature de editar orçamento), orçamentos salvos antes disso não entram em nenhuma fila — limitação aceita, sem migração, mesmo padrão já usado pra outros campos novos em dados antigos | Aprovada |

| 69 | 2026-09-21 | Duplicar orçamento: escolhida a opção (b) já cogitada no roadmap — reabrir como rascunho preenchido na aba Orçamento (reaproveitando `QuoteViewModel.loadForEditing`, decisão 64), em vez de clonar direto uma linha no Histórico. Diferença de implementação pra edição: `duplicateForNewQuote` não marca `editingQuoteId`, então "Salvar" cria uma linha nova (`QuoteHistoryRepository.save`) com `savedAtEpochMillis`/status (`ORCADO`) frescos, em vez de atualizar o original — exatamente as duas regras já definidas pelo responsável do projeto. Reaproveitamento de arquivo (foto/STL) implementado com `photoReferenceFileName`/`stlReferenceFileName` opcionais em `save`/`update`: quando presentes, o repositório usa o nome de arquivo já existente em disco em vez de regravar os bytes — `QuoteViewModel` seta essas referências ao carregar um `SavedQuote` (edição ou duplicação) e as limpa (`= null`) sempre que o usuário troca/remove a foto ou o STL manualmente, garantindo que só reaproveita o arquivo quando o anexo genuinamente não mudou. `delete()` e a limpeza de arquivo trocado durante `update()` passam a checar se outro `SavedQuote` ainda referencia aquele nome de arquivo antes de apagar — sem isso, excluir ou editar um dos dois orçamentos que compartilham foto/STL apagaria o arquivo do outro. De quebra, o mesmo mecanismo evita regravar o arquivo à toa ao editar um orçamento sem trocar a foto/STL (não só na duplicação) | Aprovada |

| 70 | 2026-09-21 | Quadro Kanban de pedidos: colocado como um alternador "Lista"/"Kanban" **dentro** da aba Histórico já existente, não uma aba nova no menu principal — evita renumerar atalhos de teclado (decisão 43/45) e mantém "uma visão a mais sobre o mesmo dado" (como o próprio roadmap já descrevia) de forma mais literal, reaproveitando a mesma busca/filtro de período. O filtro de status é escondido no modo Kanban (as colunas já cumprem esse papel; deixá-lo visível mas sem efeito seria confuso). Arrasto implementado sem nenhuma biblioteca de drag-and-drop: `Modifier.pointerInput` + `detectDragGestures` acumula o deslocamento do card durante o arrasto, e `Modifier.onGloballyPositioned { it.boundsInWindow() }` (tanto nas colunas quanto no card sendo arrastado) dá uma referência de coordenadas em comum — ao soltar, verifica em qual `Rect` de coluna o centro do card caiu. Cada card também ganhou um menu "⋮" com Editar/Duplicar/Excluir (mesmos callbacks já usados na lista) como caminho alternativo à mudança de status por arrasto — decisão deliberada por não ser possível testar visualmente a ergonomia do gesto de arrastar antes de publicar; se o arrasto não funcionar bem na prática, a funcionalidade continua plenamente utilizável pelo menu | Aprovada |

| 71 | 2026-09-21 | Correções no Quadro Kanban (decisão 70) depois de testar de verdade (feedback do responsável do projeto): **(1)** soltar o card só mudava o status perto do título da coluna, não no resto dela — causa: `LayoutCoordinates.boundsInWindow()` retorna o retângulo **recortado pela área visível de todos os ancestrais**, e o quadro Kanban vive dentro do scroll vertical da tela de Histórico; uma coluna alta (título + vários cards) parcialmente fora da parte visível da tela tinha seu retângulo relatado como só a fatia visível (geralmente só o título), então soltar mais abaixo caía fora de qualquer retângulo de coluna conhecido. Trocado por `LayoutCoordinates.positionInWindow()` + o tamanho medido (`size.toSize()`), que dá a posição/tamanho reais sem recorte de visibilidade — usado tanto pras colunas quanto pro card sendo arrastado. **(2)** Adicionado indicador visual de destino durante o arrasto: a coluna sob o ponteiro ganha um destaque de fundo (`primaryContainer` translúcido) e uma "vaga fantasma" (retângulo tracejado) no fim da lista, recalculado a cada evento de `onDrag` (não só no soltar) comparando o centro do card contra os retângulos de coluna conhecidos. **(3)** Cards do Kanban passam a mostrar a miniatura da foto do orçamento (quando existir) no topo, mesmo dado (`photoBytes`) já usado na lista — `KanbanBoard` ganhou um parâmetro `photoBytesFor: (SavedQuote) -> ByteArray?` em vez de depender do `ViewModel` diretamente, mantendo o componente sem acoplamento a essa camada, mesmo padrão dos outros callbacks (`onEdit`/`onDuplicate`/etc.) | Aprovada |

| 72 | 2026-09-21 | Segunda rodada de correções pós-teste (feedback do responsável do projeto): **(1)** Kanban ainda só aceitava soltar o card em cima de conteúdo existente (card ou título) — a correção da decisão 71 resolveu o recorte de visibilidade, mas cada coluna continuava medindo só a altura do próprio conteúdo (`Column` sem altura fixa), então o espaço "vazio" abaixo do último card (ou numa coluna curta) não fazia parte de nenhum retângulo de coluna conhecido. Corrigido esticando todas as colunas até a altura da mais alta: `Row(Modifier.height(IntrinsicSize.Max))` no `KanbanBoard` + `Modifier.fillMaxHeight()` em cada `KanbanColumn` — padrão comum do Compose pra igualar alturas de itens lado a lado, sem precisar calcular a altura manualmente. **(2)** Cards do Kanban ficaram grandes demais com a miniatura de foto (decisão 71) — o banner de largura total (100dp de altura) empurrava o resto do conteúdo pra baixo, tornando os cards mais do que o dobro do tamanho anterior. Trocado por uma miniatura pequena (48dp, cantos arredondados) ao lado do nome/cliente/preço, mesmo espírito compacto de antes da foto existir, sem abrir mão dela. **(3)** Editar um orçamento salvo levava direto pra aba Orçamento (decisão 64) sem nenhuma forma óbvia de desistir — o botão "Cancelar edição" já existia, mas só aparecia depois de rolar por todo o formulário de cálculo, dentro da seção de salvar; na prática, a única forma prática de "desistir" era salvar mesmo sem querer antes de começar outro orçamento. Resolvido sem duplicar formulário: `EditQuoteDialog` (novo, `ui/quote/`) abre o `QuoteScreen` inteiro (mesmo `QuoteViewModel`, sem estado paralelo) dentro de um `Dialog` sobre a tela de Histórico, com um botão "Cancelar edição" fixo no topo, sempre visível independente do scroll. `QuoteScreen` ganhou um parâmetro opcional `onEditingFinished` (sem efeito fora de edição), chamado ao cancelar ou ao salvar com sucesso, fechando o diálogo nos dois casos — a visibilidade do diálogo no `App.kt` deriva direto de `saveForm.editingQuoteId != null`, sem estado de UI duplicado. Esc também cancela a edição (mesmo padrão já usado pra cancelar edição de filamento/impressora/serviço). "Duplicar" continua abrindo na aba Orçamento normalmente — duplicar não sobrescreve nada existente, então o risco que motivou essa mudança para editar não se aplica ali | Aprovada |

| 73 | 2026-09-21 | Segunda leva de refinamento visual (pedida pelo responsável do projeto via `/frontend-design:frontend-design`, revisar UI/UX do app). Plano de design apresentado e aprovado antes de escrever código: evoluir a paleta já existente (azul petróleo + âmbar, decisão 35) em vez de substituí-la, e usar como fio condutor o fato de que o app inteiro existe pra produzir números precisos (preço, peso, tempo) — então esses números ganham um tratamento visual consistente de "leitura de instrumento" (`ui/format/NumericText.kt`, `FontFamily.Monospace`, sem depender de fonte externa/download, zero risco de rede ou licença) em todas as telas onde aparecem: resultado do Orçamento, Dashboard, cards do Kanban e linha do Histórico (nesta última, via `buildAnnotatedString`/`SpanStyle`, já que o valor está embutido no meio de uma frase, não numa `Text` própria). O resultado do cálculo no Orçamento (antes uma pilha de `Text` do mesmo peso: "Produção: X", "Venda: Y", "Lucro: Z", às vezes um "Total" repetindo o mesmo valor de Venda quando não havia serviço) virou uma "nota" (`QuoteReceipt`): o valor que de fato é cobrado do cliente (`QuoteResult.grandTotal` — venda + serviços, sempre não-nulo quando há orçamento, então correto mesmo sem serviços) em destaque no topo, com custo de produção/lucro/cada serviço como itens de nota abaixo — corrigindo de quebra uma duplicação (a antiga linha "Total" só aparecia com serviços selecionados, mesmo o valor de venda sozinho já sendo o que o cliente paga na ausência deles). Os 4 cartões de estatística do Dashboard (`Card` branco com sombra, um padrão exatamente do "kit SaaS genérico" que o brief da skill pede pra evitar) viram uma leitura em régua horizontal (número grande + rótulo pequeno abaixo, separados por `VerticalDivider`, sem cartão/sombra) — o item "Filamento mais usado" (um nome, não uma medida) fica de fora do tratamento monoespaçado, por ser categórico, não numérico. Status do pedido (`OrderStatus`) ganha uma cor de progresso interpolada entre `primary` (frio, recém orçado) e `secondary` (quente, entregue) — `ui/theme/OrderStatusColor.kt`, reaproveitando as cores já existentes do tema em vez de fixar hexadecimais novos, então acompanha automaticamente claro/escuro; exibida como um ponto de 8dp ao lado do título de cada coluna do Kanban e do texto "Status: X" no Histórico (lista e menu suspenso) — status vira informação visual (o pedido "esquenta" ao avançar), não decoração arbitrária | Aprovada |

| 74 | 2026-09-22 | Configurações de impressão (altura de camada, % de preenchimento, padrão de preenchimento, uso de suporte) — pedido do responsável do projeto: quem vende impressão 3D pode precisar refatiar a mesma peça depois e esquecer qual configuração usou da vez anterior. Duas perguntas resolvidas antes de implementar: **(1) campos** — conjunto básico (os quatro citados acima), sem temperatura/tipo de suporte/velocidade (dados bem menos padronizados entre fatiadores, ficariam quase sempre em branco); **(2) escopo** — só informativo (`SavedQuote.printSettings`, novo `core/model/PrintSettings`), não entra no cálculo de preço nem em nenhum export (mesmo tratamento de `sourceLink`/`client`/`stlFileName`). Extração automática: `GCodeMetadataParser` ganhou regexes pro bloco de configuração completo que PrusaSlicer/Bambu Studio/OrcaSlicer gravam no fim do G-code (`layer_height`, `fill_density`/`sparse_infill_density`, `fill_pattern`/`sparse_infill_pattern`, `support_material`/`enable_support`) — mesma limitação de fatiador do thumbnail (decisão 56): Cura não grava esse bloco, fica de fora. UI: botão "Adicionar/Editar configurações de impressão" na tela de Orçamento (fica no formulário de salvar, junto do STL/foto, por ser a mesma natureza de dado — guardado só pra recuperar depois) abre `PrintSettingsDialog` (novo, `ui/quote/`, um `AlertDialog` com os 4 campos, reaproveitável), pré-preenchido pela importação de G-code se houver dado reconhecido, editável manualmente por cima. No Histórico (lista e Kanban), o mesmo diálogo abre direto no card salvo e edita **sem** passar pela edição completa do orçamento — `QuoteHistoryRepository.updatePrintSettings` (novo, ao lado de `updateStatus`, mesmo padrão de edição rápida isolada) | Aprovada |

| 75 | 2026-09-22 | Leva 0 do plano de evolução (ver [roadmap.md](roadmap.md#leva-0--rede-de-proteção-antes-de-mexer-no-modelo-de-dados)): **(1) Backup e restauração** (`data/BackupRepository`, expect/actual como os outros repositórios). Backup é um `.zip` da pasta de dados inteira (`appDataDir()`, varredura recursiva) em vez de uma lista fixa de arquivos — assim arquivo novo criado no futuro entra no backup sozinho, sem ninguém lembrar de atualizar a lista. Na raiz do zip vai um `3dreport-backup.json` (app/versão/data), que **não** é extraído junto com os dados: serve só pra restauração reconhecer que o arquivo escolhido é um backup do 3DReport e não um `.zip` qualquer. Montado com a API de JSON em runtime (`buildJsonObject`), não com uma classe `@Serializable`, porque o módulo `composeApp` não aplica o plugin de serialização (só o `core` aplica) — evita mexer na configuração de build só por causa de um manifesto de 3 campos. **Restauração é transacional:** extrai numa pasta irmã temporária, valida, só então renomeia a pasta de dados atual pra `.3dreport-anterior-<data>` e põe a nova no lugar; qualquer falha antes disso não toca em nada, e falha na troca final desfaz o passo anterior. Os dados substituídos **não são apagados** (viram a pasta `-anterior-`), pra dar como desfazer no braço quem restaurar o arquivo errado — limpar isso fica a cargo do usuário, de propósito. Entradas do zip têm o caminho validado contra "zip slip" (`../..` gravaria fora da pasta de dados). **O app se fecha ao final da restauração** (`platform/exitApp`, novo): os repositórios carregam os dados em memória na inicialização, então continuar aberto faria a próxima gravação sobrescrever o que acabou de ser restaurado com o estado antigo — fechar é seguro porque toda alteração já é gravada em disco na hora. A alternativa (dar um `reload()` a cada repositório) foi descartada por ser mais superfície de erro justamente onde o custo de errar é perder os dados do negócio. **(2) Menu "⋮ Ações" na linha do Histórico:** a lista tinha chegado a 8 botões de texto lado a lado; ficam visíveis "Exportar PDF", "Copiar" e "Editar", e vão pro menu "Duplicar", "Configurações de impressão", "Baixar foto", "Baixar STL" e "Excluir" — mesmo padrão que os cards do Kanban já usavam (decisão 70) | Aprovada |

| 76 | 2026-09-22 | Leva 1 do plano de evolução (ver [roadmap.md](roadmap.md#leva-1--o-custo-real-do-trabalho-maior-impacto-no-bolso)), as quatro mudanças na mesma leva de propósito, pra o vendedor recalibrar a tabela de preços uma vez só: **(1) Mão de obra** (`PricingSettings.laborRatePerHour` + `PrintJob.laborMinutes`) — o custo de produção não tinha nenhuma parcela pro tempo da pessoa, então toda peça pequena e trabalhosa saía subprecificada. **(2) Acabamento deixa de ser percentual do material** quando há taxa horária configurada: lixar/pintar escala com tempo, não com gramas (uma action figure de 30 g dá mais acabamento que um suporte liso de 200 g, mas pagava 7x menos). Escolhida a **opção A** das duas registradas no roadmap: `finishingRate` continua valendo enquanto `laborRatePerHour` for zero (ninguém vê o preço mudar sozinho ao atualizar), e o campo em Configurações passa a avisar em texto quando está sem efeito — opção B (remover o campo) foi descartada por quebrar quem já usa. A chave é uma configuração do negócio (`chargesLaborByTime`), não um valor por orçamento, pra o modelo de cobrança não variar peça a peça. **(3) Reserva de falha passa a incidir sobre o custo refeito inteiro** (material + energia + manutenção + retorno da máquina + custo fixo + mão de obra + acabamento), não só sobre o material: uma impressão de 8 h que falha no fim desperdiça tudo isso. `administrativeCost` fica de fora por ser a única parcela que não se refaz (modelagem já entregue continua pronta). Essa é a única mudança da leva que altera o preço de quem não configurar nada — deliberada, e registrada no CHANGELOG em linguagem de usuário. **(4) Custo fixo mensal do negócio** (`monthlyFixedCost` ÷ `productiveHoursPerMonth`, exposto como `fixedCostPerHour`), reaproveitando exatamente o padrão que `MachineInvestment.costPerHour` já usava pra diluir o valor da máquina, agora pro negócio inteiro. Os três campos novos nascem zerados e zero desliga a parcela, então atualizar o app não muda preço em silêncio. **Fora de escopo, registrado no roadmap:** minutos de trabalho embutidos em cada `Service` — mexeria na decisão 25 (serviço só entra no total, não no custo/lucro) e merece decisão própria. Documentação de usuário atualizada junto (obrigatório, não opcional): `docs/pricing-formulas.md` ganhou a seção "Duas regras que mudaram" e um segundo exemplo de referência com mão de obra e custo fixo (travado por teste, pra doc e código não divergirem), mais site (fórmula, FAQ visível e JSON-LD), manual, README e o diálogo de Ajuda do app | Item (2) substituído pela 93 |

| 77 | 2026-09-22 | Leva 2 do plano de evolução (quantidade e lote). **Semântica das entradas, a decisão mais importante aqui:** comprimento/tempo/minutos de trabalho continuam sendo de **uma peça**, e o app multiplica por `quantity`. A alternativa (tratar os campos como já sendo do lote inteiro) foi descartada pelo modo de falha: se o usuário informar dados de uma peça e o app achar que são do lote, ele cobra 1 e entrega 10, perdendo 90% do dinheiro **em silêncio**; no sentido contrário o erro vira um preço 10x absurdo, que salta aos olhos. Entre dois enganos possíveis, escolhido o que aparece. A tela diz isso em texto, com a saída explícita pra quem fatia a mesa cheia de uma vez (manter quantidade 1). **Quantidade e preparo ficam em `Quote`, não em `PrintJob`**, porque descrevem o pedido e não a peça; `PrintJob` continua sendo "uma unidade". **Todos os valores de dinheiro e massa do `Quote` passam a ser do pedido inteiro** (já multiplicados), com `unitSalePrice` derivado — o contrário faria Dashboard, PDF e Histórico subnotificarem por um fator igual à quantidade, cada um no seu canto. **Economia de lote sem desconto artificial:** `Quote.setupMinutes` (preparar arquivo, fatiar, montar mesa) é cobrado uma vez pelo pedido, enquanto `PrintJob.laborMinutes` multiplica — o preço por unidade cai sozinho porque o preparo realmente é feito uma vez. Por isso **o desconto percentual por volume que o roadmap citava foi adiado pra leva 4** (negociação): ter dois mecanismos de desconto ao mesmo tempo, um de custo e um de preço, confundiria mais do que ajudaria. **Serviços multiplicam pela quantidade** (pintura/lixamento/embalagem são trabalho por peça), exibidos como "× N" no resultado e nos exports pra não virar surpresa; registrado no roadmap que serviços por pedido (ex.: entrega) podem precisar de um marcador próprio se aparecer caso real. `PrintQueueReport` passou a multiplicar o tempo pela quantidade, senão a fila subnotificaria. Documentação atualizada junto: nova seção "Quantidade e lote" em pricing-formulas.md (com a tabela do que multiplica e o que não multiplica, travada por teste), CHANGELOG, site e manual | Aprovada |

| 78 | 2026-09-22 | Leva 3 do plano de evolução (o preço final correto). **Canal de venda e forma de pagamento viram um campo só, contrariando o roadmap de propósito:** o roadmap pedia "canais de venda" e "taxa de pagamento" como itens separados, mas somar "Shopee 20%" com "cartão 4%" cobraria em dobro, porque a taxa do marketplace já embute o processamento do pagamento. Cada [SalesChannel] cadastrado é uma taxa só ("Shopee" 20%, "Cartão" 4%, "Pix" 0%), escolhido por orçamento; quem paga os dois de verdade (loja própria com maquininha) cadastra um canal com a soma. **Catálogo em Configurações, não em aba nova** — mesmo critério da decisão 47 (Templates): uma aba fixa seria desproporcional pra algo configurado uma vez, e mexer na contagem de abas mexe nos atalhos de teclado (decisões 43/45). **Migração automática:** na primeira execução sem `channels.json`, se `PricingSettings.marketplaceFeeRate` estiver preenchido, o repositório cria um canal "Marketplace" com a mesma taxa — atualizar o app não pode zerar em silêncio uma configuração que já afetava preço. O campo antigo fica como legado (não é mais lido pelo cálculo, mas continua gravado pra não quebrar quem ainda não abriu o app). **`Quote.marketplaceFeeRate` mantém o nome** mesmo agora significando "taxa do canal": renomear faria orçamentos já salvos perderem a taxa na desserialização e passarem a exibir um lucro maior do que o real. Somado a ele, `Quote.taxRate` (novo) e `totalDeductionRate` derivado. **Imposto é percentual e MEI não entra nele:** o DAS do MEI é valor fixo mensal, então o lugar dele é `monthlyFixedCost` (leva 1), e a tela diz isso em texto; o campo de imposto serve pro Simples Nacional e afins. **Deduções somam antes de dividir** (`venda = base / (1 − canal − imposto)`), com `require` recusando soma ≥ 100% e mensagem em português que aparece direto na tela de Orçamento pelo caminho de erro que já existia. **Frete não passa por nada:** `SavedQuote.shippingCost` entra como linha própria no total, não multiplica pela quantidade, não passa pela margem e não sofre dedução, porque é repasse e não produto do vendedor. **"Frete grátis" ficou de fora** (estava no roadmap): exigiria recalcular lucro fora do `PricingCalculator`, e ter dois números de lucro no app é como relatórios passam a divergir entre si — registrado no roadmap pra ter decisão própria. Documentação atualizada junto: nova seção "Deduções da venda e frete" em pricing-formulas.md (com a tabela do que multiplica/passa pela margem/sofre dedução e um exemplo travado por teste), CHANGELOG, site e manual | Aprovada |

| 79 | 2026-09-22 | Leva 4 do plano de evolução (negociação). **O preço fechado com o cliente vira o `Quote.salePrice` de verdade, não uma simulação à parte** (`PricingCalculator.calculate(negotiatedSalePrice = ...)`, um parâmetro opcional só): se o vendedor fecha por R$ 30 e o histórico guarda os R$ 38,91 da tabela, o Dashboard passa a somar um faturamento que nunca existiu. Como `Quote.profit` já deriva de `salePrice`, lucro, margem, exports e relatórios acompanham sozinhos, sem nenhum caminho paralelo de cálculo. O alvo digitado é o **total do cliente**, então serviços e frete saem antes de sobrar o preço da peça; alvo menor que os extras deixa a peça em zero e o prejuízo aparece no lucro, que é o próprio aviso. **Aviso de prejuízo e preço mínimo:** `Quote.breakEvenSalePrice` (produção ÷ (1 − deduções)) e `actualProfitMargin` entram no modelo em vez de virarem conta solta na tela, porque são a mesma matemática de dedução que o motor já faz. O mínimo fica sempre visível, não só quando o preço fica ruim: é o número que o vendedor precisa ter na cabeça durante a conversa. **Desconto percentual por volume, adiado da leva 2, foi descartado:** quem quer dar desconto digita o preço fechado e vê o efeito na hora; ter duas formas de chegar ao mesmo número (um percentual e um valor) só criaria divergência. **Comparar impressoras** roda o mesmo cálculo trocando só a máquina (`QuoteViewModel.comparePrinters`, reaproveitando `calculate`), só aparece com duas ou mais cadastradas, e destaca a mais barata. Sem `Quote` novo nem campo persistido: é leitura derivada, recalculada junto com o resto. Registrado no roadmap como possível evolução guardar que o preço foi negociado (pra responder "quanto de desconto eu dei no mês"), o que hoje não é dedutível do dado salvo | Aprovada |

| 80 | 2026-09-22 | Leva 5 do plano de evolução (saída pro cliente), no Histórico, dentro do menu "⋮ Ações" que a leva 0 criou. **(1) Abrir no WhatsApp:** link `wa.me` montado em código comum (`ui/history/WhatsAppLink.kt`), com o mesmo texto do copiar/colar codificado em percent-encoding escrito à mão, porque o Kotlin Multiplatform não tem codificador de URL comum e o texto tem acento e quebra de linha, que quebram o link crus. Telefone normalizado a partir do contato digitado à mão: 10 ou 11 dígitos viram número brasileiro com o 55 na frente, 12 a 15 vão como estão, e qualquer outra coisa (e-mail, @ de rede social, vazio) resulta em **link sem número** em vez de esconder o botão — o WhatsApp abre perguntando pra quem enviar, e a mensagem já está escrita de qualquer forma. Nada de API oficial nem serviço externo: é só abrir uma URL, reaproveitando `platform/openUrl`, então continua 100% local. **(2) Imagem quadrada 1080×1080** (`platform/QuoteImageExporter`, expect/actual como o PDF): desenhada com `Graphics2D` sobre `BufferedImage` no JVM, decodificando a foto por Skia (`decodeImageBitmap`) e **não** por `ImageIO`, pelo mesmo motivo da decisão 65 — `ImageIO` devolve `null` em silêncio pra WebP e a foto sumiria sem erro nenhum. Foto preenche a área cortando o excesso em vez de esticar (peça distorcida passa impressão errada do produto); nome comprido é cortado com reticências pra nunca vazar da arte; **sem foto, o texto vai centralizado e a faixa some**, senão sobrariam um retângulo vazio e uma linha de separação sem nada pra separar (visto na primeira renderização e corrigido). Mostra só o que o cliente já veria no PDF; produção, lucro, link e cliente continuam fora. Prazo de entrega ficaria bem na arte, mas **não existe campo de prazo no modelo** — registrado no roadmap em vez de inventado. O texto de marca reaproveita a marca d'água do PDF (`BrandingSettings.watermarkText`), pra o material do vendedor sair coerente entre PDF e imagem | Aprovada |

| 81 | 2026-09-22 | Leva 7 do plano de evolução (UI/UX), **três dos quatro itens**: **(1) Tela de Orçamento em duas colunas** (`BoxWithConstraints`, entradas + formulário de salvar à esquerda, resultado/negociação/comparação à direita, cada coluna com rolagem própria). Abaixo de 1100dp de largura a tela volta pra coluna única, que é o layout que sempre funcionou — responsivo de verdade, não um layout fixo que quebra em janela pequena. A janela padrão subiu de 960×720 pra 1280×820, senão o layout novo nunca apareceria sem a pessoa redimensionar na mão. O arquivo foi dividido em `QuoteInputs`/`QuoteResultSection`/`SaveQuoteFormSection` pra as duas montagens (uma e duas colunas) compartilharem o mesmo conteúdo em vez de duplicá-lo. **(2) Barra de composição do preço** na nota: barra empilhada + legenda com material, energia, máquina (manutenção + retorno + custo fixo), seu trabalho (mão de obra + acabamento), reserva de falha, administrativo e lucro. Agrupada assim, e não uma fatia por campo do `CostBreakdown`, porque sete fatias finas viram enfeite: o que ensina a precificar é ver que a máquina pesa mais que o plástico. Fatias zeradas são omitidas (`Modifier.weight` não aceita zero, e uma legenda de "R$ 0,00" atrapalha). **(3) Onboarding de três perguntas** (`OnboardingRepository` + `OnboardingDialog`), com o estado num arquivo próprio e **não** num campo de `PricingSettings`: não é parâmetro de custo, e misturar faria um backup de configurações levar junto o "já vi isso" de outra máquina. **Perguntas mudadas em relação ao roadmap:** ele pedia impressora, kWh e margem; virou kWh, **valor da hora de trabalho** e margem. Impressora e filamento já vêm com cadastro padrão utilizável e têm catálogo pronto nas próprias telas, enquanto a hora de trabalho nasce zerada e, sem preencher, faz o maior custo do vendedor sumir do preço (leva 1) — perguntar isso vale muito mais no primeiro uso. **Sugestão de kWh por estado foi descartada:** tarifa muda por distribuidora e por reajuste anual, então uma tabela embutida nasceria desatualizada e erraria pra boa parte das pessoas; o texto manda conferir na conta de luz, que é a fonte certa. Mesmo critério de não inventar número usado no preset de impressoras (decisão 54). **(4) Unificar as telas de catálogo e migrar pra Scaffold/Snackbar ficou de fora**, proposto como leva própria: é refactor interno grande, muda a navegação (uma aba a menos) e mexe na numeração dos atalhos de teclado (decisões 43/45), então merece decisão separada em vez de entrar de carona numa leva de melhorias visuais | Aprovada |

| 82 | 2026-09-23 | Último item da leva 7, **reduzido depois de conferir a premissa** (decisão 81 já tinha tirado ele da leva). O item juntava três coisas: fundir as abas de catálogo, unificar o código das três telas e trocar feedback inline por Snackbar. Só a terceira foi feita. **Unificação do código descartada:** o roadmap registrava em 2026-09-16 que Filamentos/Impressoras/Serviços eram "quase idênticas", o que deixou de ser verdade — hoje são 362, 199 e 142 linhas, com 9, 8 e 3 campos, e cada uma ganhou conteúdo próprio (paleta de cores e chips de marca/tipo pelas decisões 39/40 e 54, diálogo de preset e fila de impressão pelas decisões 54 e 68). O que sobrou em comum é uma casca fina (título, lista, formulário, `ConfirmDialog`), e um componente genérico precisaria de tanta parametrização que ficaria mais difícil de ler que as três telas separadas. **Fusão das abas descartada:** o argumento era liberar espaço na barra de 7 abas, mas a decisão 81 alargou a janela padrão pra 1280dp, onde as 7 cabem folgadas; em troca custaria um clique a mais nas telas de uso diário e renumeraria os atalhos `Ctrl+4` a `Ctrl+7`, que é memória muscular de quem já usa. **Snackbar feito**, com duas escolhas: (1) **só confirmações de sucesso** viram aviso transitório; erro de validação continua inline, porque precisa ficar ao lado do campo enquanto a pessoa corrige, e um aviso que some sozinho é o oposto disso; (2) **sem migrar pra `Scaffold`** — o `SnackbarHost` entrou como sobreposição num `Box` dentro do `Surface` que já existia, porque os slots do Scaffold (barra superior, inferior, FAB) não são usados: o app tem barra de abas e rodapé próprios, e trocar tudo por Scaffold seria refazer um layout que funciona pra ganhar nada visível. Os ViewModels ganharam `consume...Confirmation()` pra desmarcar a flag depois do aviso, senão salvar duas vezes seguidas sem editar nada no meio não avisaria a segunda vez. Erro cometido e corrigido na verificação: consumir a flag **antes** de mostrar trocava a chave do `LaunchedEffect` e cancelava a própria corrotina, engolindo o aviso | Aprovada |

| 83 | 2026-09-23 | **Ideias de fora entram por issue, e o roadmap continua sendo a única fonte de verdade.** Motivo: João Antonio testou o app e mandou 6 ideias num arquivo novo, `docs/external-ideas.md`, criado à parte pra não bagunçar o padrão das levas. A triagem mostrou que 3 das 6 já estavam no roadmap, e quem sugeriu não tinha como saber, porque não havia porta de entrada formal. Um arquivo paralelo de ideias vira uma segunda fonte de verdade, que é o que o roadmap evita. **Processo:** (1) uma ideia por issue, pelo template "Sugerir uma funcionalidade" (label `triagem`); (2) o responsável do projeto faz a triagem com um de quatro resultados: **aceita** (item novo no roadmap), **mesclada** (complementa um item existente), **já existe** (a issue é fechada apontando o item) ou **descartada** (o motivo fica no roadmap, pra não voltar à fila sem argumento novo); (3) crédito no texto do item, "(sugerido por Fulano na issue #N, data)"; (4) a issue fica aberta enquanto o item estiver pendente. Detalhe em `CONTRIBUTING.md`. `docs/external-ideas.md` foi removido; o conteúdo segue no histórico do git e, com crédito, no roadmap. **Triagem das 6 ideias:** (a) **PDF com logo, contato e borda**: aceita como item novo da leva 5, estendendo `BrandingSettings`, com o rodapé desenhado junto da assinatura "Gerado com 3DReport"; (b) **ícones nos títulos**: aceita na leva 7, **revisando a decisão 54**. A 54 perguntou "fica mais bonito?"; a pergunta certa é "fica mais fácil de usar?". A Nielsen Norman Group mostra que quase nenhum ícone é universal, mas que ícone **junto do texto** ajuda a varrer a tela e reconhecer o que já foi visto, e o Material Design 3 prevê ícone + rótulo em abas. Os dois motivos da 54 caem: a inconsistência some com aplicação sistemática em todas as telas (a unificação que era o gatilho de revisão foi descartada na decisão 82), e a dependência se evita copiando Material Symbols (Apache 2.0) como vetores. Escopo: abas, títulos de seção das telas longas e ações do Histórico/Kanban; nunca ícone sozinho nem em cada campo. Os ícones do domínio 3D (impressora 3D, carretel) precisam ser desenhados pro projeto, porque o "print" do Material é impressora de papel; validar com teste de reconhecimento com 3 a 5 pessoas; (c) **imagens das impressoras**: mesclada no item de presets, que já tinha a licença das fotos como decisão em aberto, com uma terceira alternativa (foto da própria impressora, enviada pelo vendedor); (d) **tempo de impressão e opções rápido × qualidade**: dividida em dois itens da leva 5, tempo de fabricação no PDF (pequeno; tempo não é custo, a decisão 19 não impede) e orçamento com variantes (maior, mexe em `SavedQuote`, precisa de decisão própria de modelo); (e) **prazo de entrega**: já existia na leva 5 (decisão 80), anotada a segunda fonte de demanda e o destaque no PDF; (f) **aba Manutenção**: mesclada aos dois itens de manutenção que já existiam e promovida pra leva 6, com alertas por componente e registro de horas fora de orçamento, mas **sem aba nova**: fica na aba Impressoras, onde está a fila (decisão 68), pra não mexer na barra e nos atalhos defendidos na decisão 82. Correção de passagem: o item antigo "Onboarding na primeira execução" foi marcado como feito (decisão 81) | Aprovada |
| 84 | 2026-09-23 | Último item aberto da leva 4 (negociação): **guardar que o preço foi negociado.** A decisão 79 deixou isso esperando um pedido concreto, e ele apareceu como bug: `loadForEditing` não restaurava o preço fechado, então reabrir um orçamento negociado e salvar de novo trocava em silêncio o valor combinado pelo de tabela, mudando o faturamento do Dashboard. **Campo nullable, não booleano:** `Quote.tableSalePrice` guarda o preço que a margem daria só quando houve negociação (`null` sem negociação e em orçamentos antigos). Um booleano diria que houve desconto, mas não quanto; o preço de tabela responde as duas perguntas com um campo só. `isNegotiated` e `negotiatedDiscount` derivam dele no modelo, e nenhuma conta de lucro usa o preço de tabela (o lucro continua saindo só de `salePrice`, como na decisão 79). **Soma líquida:** preço fechado acima da tabela entra como desconto negativo, então "Descontos dados" no Dashboard (`QuoteSummary.totalNegotiatedDiscount`, só aparece com ao menos um negociado no período) sempre fecha com o faturamento. **Uso interno:** a marca aparece no card do Histórico e nunca em PDF, imagem, copiar/colar ou WhatsApp. Editar e duplicar restauram o preço fechado no campo (as duas usam `inputStateFrom`). Orçamentos salvos antes desta versão não têm como ser identificados como negociados e contam como preço de tabela. O ranking de clientes que mais puxam o preço pra baixo ficou de fora; cabe na leva 6 se fizer falta | Aprovada |
| 85 | 2026-09-23 | Leva 5 do plano de evolução (saída pro cliente), dois dos três itens pendentes: **prazo de entrega** e **tempo de impressão no orçamento do cliente**. **(1) Prazo como data fixa, não "N dias após aprovar":** `SavedQuote.deliveryDateEpochDay` (dias desde 01/01/1970, e não millis, pra data de calendário não mudar de dia com o fuso). "Dias úteis após aprovação" nunca venceria, mas cada vendedor conta prazo de um jeito e nem todo mundo segue um padrão tão rígido; a data é o que o cliente lê e é o que permite ao app **avisar atraso**. A pessoa continua pensando em dias: o seletor (`ui/quote/DeliveryDatePicker`) tem atalhos "+3 / +7 / +15 dias", um calendário (que não deixa escolher data passada) e "Sem prazo". **O custo de guardar data é envelhecer**, e isso foi tratado em quatro frentes: duplicar um orçamento **não copia** o prazo; o card da lista e do Kanban mostra uma etiqueta (`DeliveryBadge`) que distingue **atraso** (aprovado/imprimindo/pronto com data passada, em cor de erro) de **orçamento parado com prazo vencido** (ainda Orçado, em cor neutra, "atualize antes de reenviar"); o menu "⋮" ganha "Alterar prazo de entrega", que muda só a data sem abrir a edição completa (`QuoteHistoryRepository.updateDeliveryDate`, mesmo molde de `updatePrintSettings`); e **todo envio pro cliente que mostra o prazo** (PDF, PDF em lote, imagem, WhatsApp, copiar) passa por um aviso quando a data já passou, com "Alterar prazo" / "Enviar assim mesmo" (em lote, só "Cancelar" / "Enviar assim mesmo", porque não dá pra editar vários prazos num diálogo só). O aviso vale pra qualquer status antes de Entregue, e não só Orçado como previsto no plano: num pedido já aprovado, mandar a data velha é justamente o atraso que o cliente vai notar. O catálogo não mostra prazo (é vitrine, prazo é do pedido) e por isso não passa pelo aviso. **Destaque no PDF:** faixa âmbar clara com filete âmbar e texto em negrito logo depois do total (pedido do teste externo, decisão 83); no texto e no WhatsApp, "Prazo de entrega: até 30/09/2026" no fim; na imagem quadrada, "Entrega até 30/09" em âmbar abaixo do preço, com a faixa inferior passando a ter altura calculada pelas linhas, pra não estourar quando também há preço unitário. **Dica da fila, sem preencher nada:** ao escolher o prazo, a tela mostra quanto tempo de impressão está na frente na impressora escolhida (`PrintQueueReport.summarize` ganhou o parâmetro `statuses`, padrão só Em impressão pra aba Impressoras continuar igual; a dica usa Aprovado + Em impressão e ignora o próprio orçamento em edição). É a metade da conta de um prazo honesto; a outra metade (quantas horas por dia a máquina roda, quanto tempo leva o acabamento) o app não sabe, então não sugere data. **(2) Tempo de impressão:** opção global `BrandingSettings.showPrintTime` (e em `QuoteTemplate`, porque é escolha de apresentação), **desligada por padrão**: é tempo de máquina, não custo nem margem, então não fere a decisão 19, mas pode virar argumento de barganha ("só 2 h de máquina?"). Rótulo **"Tempo de impressão"**, e não "de fabricação" como no pedido original: o número é tempo de máquina, e "fabricação" sugeriria um prazo que ele não é. Vale pro pedido inteiro (tempo de uma peça × quantidade, `SavedQuote.totalPrintTimeMinutes`, a mesma conta da fila), com "(N peças)" quando há mais de uma, e aparece no PDF e no texto/WhatsApp, mas não na imagem, que continua só com foto, preço e prazo. Formato "6 h 30 min" (`minutesToDurationText`) em vez de "6,5 h", que é como se fala. **O PDF ganhou `PdfLayoutOptions`**, um objeto de opções com padrão no fim da assinatura, em vez de mais um parâmetro solto: os testes existentes não mudaram e a leva seguinte (identidade do vendedor) cabe no mesmo objeto. Com os padrões e sem prazo, PDF, texto e imagem saem idênticos aos de antes. **Orçamento com variantes (rápido × acabamento fino) continua fora**, esperando decisão própria de modelo de dados | Aprovada |
| 86 | 2026-09-23 | Leva 5 do plano de evolução, item **identidade do vendedor no PDF: logo, contato e borda** (teste externo, decisão 83). **Identidade e apresentação separadas:** `BrandingSettings` ganha `logoFileName`, `contactWhatsApp`, `contactEmail`, `contactInstagram` e `showBorder`, mas `QuoteTemplate` ganha **só** `showBorder`. Logo e contato são a identidade de quem vende, uma só; o template guarda aparência. Sem esse corte, aplicar um template apagaria a logo (e `TemplateListViewModel.load` montava um `BrandingSettings` do zero, o que já apagaria qualquer campo novo): agora ele parte da configuração atual e só troca os campos de template, e o indicador "Ativo" compara só esses campos. **Contato fixo em três campos**, e não texto livre: cobre o que o vendedor brasileiro usa, sai formatado de um jeito só (`contactLines`, com o "@" do Instagram garantido) e deixa o Instagram disponível sozinho pra imagem. É contato **público**, diferente de `Client.contact`, que é interno (decisão 37), e a tela diz isso no rótulo. `watermarkText` **mantém o nome** (mesmo motivo de `marketplaceFeeRate`: renomear perderia o valor de quem já configurou) e na tela vira "Nome da sua marca". **Logo como arquivo à parte** (`branding/logo-<timestamp>.<ext>` na pasta de dados), por isso entra no backup sem tratamento especial (teste confirma que volta na restauração). `BrandingRepository.update(branding, LogoChange)` com `Keep`/`Replace`/`Remove`: grava o arquivo novo, depois a configuração, e **só então** apaga o antigo, pra uma falha no meio nunca deixar a configuração apontando pra um arquivo inexistente. Arquivo sumido da pasta faz o PDF sair sem logo, sem erro, e Configurações avisa. A logo é validada **ao escolher** (decodificando por Skia, que lê WebP, decisão 65), e não ao exportar. **Cabeçalho de papel timbrado** só quando há logo ou contato (o nome sozinho já tem marca d'água e rodapé): logo à esquerda (no máximo 140×56 pt; sem logo, o nome em destaque), nome e contatos alinhados à direita, linha fina embaixo. Sem logo, contato e borda, o PDF sai idêntico ao anterior. Vale também pro catálogo, que mede o cabeçalho antes pra saber quantas linhas de produto ainda cabem. **Logo embutida uma vez por documento**, reduzida a no máximo 512 px e reaproveitada em todas as páginas: convertida por página, um PDF em lote de 20 orçamentos carregaria a logo 20 vezes. **Borda** fina a 20 pt da página, com o rodapé subindo alguns pontos pra não encostar. **Posição da assinatura "Gerado com 3DReport" (leva 8) reservada:** o contato fica no cabeçalho, e não no rodapé, justamente pra o rodapé continuar só com o nome centralizado e o canto inferior direito livre pra assinatura, sem os dois disputarem espaço. **Emoji não derruba mais o PDF (`pdfSafe`):** as fontes padrão do PDF só conhecem a tabela WinAnsi, e um emoji no nome do orçamento lançava exceção e cancelava a exportação inteira; todo texto do PDF agora passa por um filtro que descarta o caractere que a fonte não desenha. Era um risco antes desta leva, e os campos de contato digitados à mão o tornariam mais provável. **Imagem quadrada:** a linha de marca vira "Nome · @instagram" (sem Instagram, o WhatsApp), numa pílula escura translúcida por cima da foto, porque texto claro sumia em foto clara. **A logo não vai pra imagem:** o fundo é azul-petróleo escuro, e a logo típica (escura com fundo transparente) desapareceria nele. **"Ver como fica" em Configurações** gera o PDF de verdade de um orçamento de exemplo com o que está no formulário, mesmo sem salvar, e mostra a primeira página rasterizada pelo próprio PDFBox (`renderPdfFirstPagePng`): uma prévia desenhada em Compose seria uma segunda implementação do layout, que cedo ou tarde divergiria do PDF. Histórico e prévia resolvem marca, logo e contato pela mesma função (`BrandingSettings.resolvePdfBranding`). **Validação ajustada:** nome preenchido sem marca d'água nem rodapé só é erro quando também não há logo nem contato, porque nesse caso o cabeçalho mostra o nome | Aprovada |
| 87 | 2026-09-23 | Leva 7 do plano de evolução, último item: **ícones junto dos textos** (teste externo, decisão 83, que revisa a 54). **Regra:** ícone só ao lado de texto, nunca no lugar dele, decorativo pro leitor de tela (`contentDescription = null`). **Escopo decidido com o responsável do projeto:** abas, títulos de seção (Configurações e Orçamento, incluindo os subtítulos "O que o cliente vê"/"Só pra você"/"Sua marca"/"Aparência do PDF") e ações do Histórico/Kanban (botões e menu, que passou de "⋮ Ações" pra ícone + "Ações"). **Campos de formulário ficam sem ícone**, inclusive "Filamento" na tela de Orçamento, apesar de o exemplo original do pedido ser exatamente esse ("rolinho ao lado de Filamento"): o roadmap já tinha tirado campos do escopo, e o responsável confirmou. **Sem dependência nova:** os ícones genéricos são Material Symbols (Outlined, peso 400, grade 24) copiados como vetor do repositório oficial pra `ui/icons/AppIcons.kt` (`ImageVector` montado com `addPathNodes`, só na primeira vez que aparece), em vez da biblioteca de ícones do Compose; licença Apache 2.0, registrada em `THIRD-PARTY-NOTICES.md`. **Ícones do domínio desenhados pro projeto:** o "print" do Material é impressora de papel e não existe carretel pronto. A impressora é uma **caixa fechada** com bico e peça na mesa (a primeira versão, pórtico aberto, parecia uma porta). O carretel foi escolhido entre **cinco variantes renderizadas em 24 px**: de frente, com anéis concêntricos, parecia alvo ou lupa (e se confundiria com "buscar"); de lado, com as abas retas, parecia escada; ficou a vista em 3/4, com a face da frente, o furo do cubo e a borda de trás dando profundidade. **Abas:** ícone à esquerda do texto (`LeadingIconTab`) e não em cima, pra barra continuar com 48 dp; contornado nas inativas e preenchido na ativa (padrão M3). **Abaixo de 1160 dp de largura, as abas voltam a ser só texto:** com ícone, os rótulos quebravam no meio da palavra ("Orçament/o") numa janela de 900 dp, e é o ícone, que é apoio, que sai quando falta espaço. **Escolhas registradas:** Backup usa uma caixa de arquivo, **não nuvem**, porque o app é 100% local e o ícone de nuvem prometeria o contrário; WhatsApp usa um balão de conversa genérico, **não a marca**, que é de terceiros (mesmo cuidado da decisão 54 com fotos de impressora); o mesmo ícone se repete quando o assunto é o mesmo (Serviços na aba e na seção; impressora na aba e em "Comparar impressoras"), que é o que faz dele uma âncora. **Validação:** o item fechou depois do teste de reconhecimento, feito e aprovado em 2026-09-24 (nenhum ícone trocado), planejado com 3 a 5 pessoas, incluindo quem sugeriu, com a folha `docs/assets/teste-reconhecimento-icones.png` (os seis ícones mais arriscados, numerados e sem texto, em tamanho real e ampliados). Ícone não reconhecido se troca só no `AppIcons`, sem mexer em tela | Aprovada |
| 88 | 2026-09-23 | Leva 8 do plano de evolução, item **assinatura "Gerado com 3DReport" nos PDFs** (a maior alavanca de divulgação de um projeto gratuito e sem verba, decisão 15). **Onde:** canto inferior direito, na mesma linha do rodapé, que é o espaço que a decisão 86 deixou reservado; 7 pt em cinza claro, alinhada ao fim da linha do rodapé e dentro da borda quando ela existe. O texto inteiro é um link (`PDAnnotationLink` + `PDActionURI`) **só pro site**: o Buy Me a Coffee, citado na ideia original, ficou de fora por decisão do responsável do projeto, porque pedir doação num documento que vai pro cliente do vendedor soa invasivo, e o site já tem a seção de apoio. Vale pro orçamento, pro PDF em lote e pro catálogo; **não** entra na imagem quadrada, no copiar/colar nem no WhatsApp, que são conversa, onde um link a mais seria intrusivo. **Ligada por padrão, inclusive pra quem atualiza:** `BrandingSettings.showAppSignature = true`, e como o campo falta no JSON antigo, quem atualiza recebe ligado. Pra nada mudar no documento do cliente sem o vendedor saber, quem já usava vê **um aviso único** ("Novidade nos seus PDFs", com Ver como fica / Desligar / Manter), guardado num `NoticesRepository` próprio (conjunto de identificadores de avisos já vistos, pro próximo aviso não precisar de campo nem arquivo novo). Instalação nova não vê o aviso: concluir ou pular o onboarding já o marca como visto, pra não abrir dois diálogos seguidos; a opção fica à vista em Configurações → Documentos pro cliente → Aparência do PDF, e "Ver como fica" mostra a assinatura. "Desligar" no aviso grava só essa opção a partir do que está salvo, e não do rascunho do formulário. **Não entra no template:** é preferência do app, não aparência de um documento, e trocar de template não deve religá-la. `PdfLayoutOptions.showAppSignature` nasce desligado, então quem chama o renderizador sem opções (testes, prévias antigas) recebe o PDF de sempre; quem liga é a resolução a partir de `BrandingSettings` | Aprovada |
| 89 | 2026-09-23 | Leva 8 do plano de evolução, item **arrastar o G-code na janela e sair um orçamento completo**. **Arquivos reais primeiro** (exigência do roadmap, mesma disciplina das decisões 55 e 56): G-codes publicados em repositórios abertos, exportados por **Bambu Studio 02.03 e 02.04, OrcaSlicer 1.6.3, PrusaSlicer 2.4, SuperSlicer 2.3 e Cura 5.3.1**, com as linhas de comentário relevantes copiadas pra `RealGCodeFixtures` (origem e commit de cada um no comentário). Conferido neles: a família Prusa/Orca/Bambu grava `printer_model`, `printer_settings_id`, `filament_type`, `filament_vendor` e `filament_colour`, um valor por extrusor separado por ";"; `printer_model` às vezes é um identificador interno, até com erro de digitação ("Voron_v2_300_aferburner"), por isso `printer_settings_id` também é usado, limpo do nome de projeto que o PrusaSlicer embrulha em volta, do "- Copy" e do bico; a marca muitas vezes vem como marcador genérico ("Generic", "(Undefined)", "(Unknown)"), tratado como "sem marca"; **Cura não grava tipo nem marca de filamento** e só tem a impressora dentro do JSON de `;SETTING_3` (quebrado em várias linhas), como `machine_name` (quando o perfil mexeu no nome) ou `definition` (sempre). **Bug achado pelos arquivos reais:** o Bambu Studio 2.x grava "model printing time: ...; total estimated time: ..." numa linha só, e o parser exigia o rótulo no começo da linha — **o tempo de impressão do Bambu Studio atual não era importado**. Corrigido aceitando o rótulo no meio da linha e usando o tempo total (inclui o preparo, que também ocupa a máquina). **Casamento com os catálogos** (`core/slicer/CatalogMatcher`, função pura), com **precisão antes de alcance**, porque escolher a máquina ou o filamento errado muda o preço sem o vendedor perceber: impressora só é escolhida com nome **igual** depois de normalizar (maiúsculas, espaços, pontuação), que é como os presets do app já nomeiam (decisão 54: "Bambu Lab X1 Carbon"); nome que só contém o outro vira **sugestão na mensagem**, nunca escolha ("Bambu Lab A1" e "A1 mini" são máquinas diferentes, com consumo diferente). Filamento: tipo igual ao `materialType`, só entre os com estoque; marca conhecida precisa bater com a cadastrada (outra marca tem outro preço); com vários candidatos, mantém o já escolhido se for um deles, senão pede pra escolher; **materiais diferentes em extrusores diferentes não são reduzidos a um** e a mensagem avisa (o multimaterial de verdade é a leva 9). Cor: a cadastrada mais próxima da cor do G-code, só se for parecida (distância RGB até 100) e o G-code usar uma cor só; filamento com uma cor só em estoque dispensa a comparação. **Arrastar:** `Modifier.fileDropTarget` (expect/actual sobre o drag-and-drop do Compose Desktop) na janela inteira, com um aviso por cima enquanto um arquivo é arrastado; ao soltar, vai pra aba Orçamento e passa pelo mesmo `importGCode` do botão, que também passou a casar impressora e filamento. A mensagem de importação diz o que foi escolhido e o que não bateu (e por quê). "Desfazer importação" devolve também a impressora, o filamento e a cor anteriores. `.bgcode` (binário do PrusaSlicer), `.3mf` e arquivo que não é G-code recebem mensagem com o caminho pra resolver. **Fora:** o `.gcode.3mf` do Bambu Studio (um zip com o G-code dentro) ficou registrado no roadmap, porque nenhum exemplo público confirmou a estrutura a tempo, e a regra é não implementar formato imaginado | Aprovada |
| 90 | 2026-09-23 | Leva 8 do plano de evolução, item **reposicionar a comunicação** de "calculadora de orçamento" pra **"o sistema de quem vende impressão 3D"**. Hero do site, abertura do README e manual passam a liderar com as duas pernas de diferenciação da revisão de 2026-09-22: **o sistema completo** (arrastar o G-code → custo real com imposto e canal → negociação → prazo → WhatsApp/PDF com a marca do vendedor → Kanban/fila/Dashboard) e **gratuito e 100% no computador** (sem conta, sem mensalidade, seus preços e clientes não saem da máquina), contra concorrentes SaaS de mensalidade. Seção nova no site, "100% no seu computador", incluindo o que é brasileiro de verdade (MEI/Simples, Shopee/Mercado Livre, maquininha/Pix, WhatsApp, R$); funcionalidades reorganizadas pela ordem da venda; FAQ nova sobre o G-code (a da assinatura veio na decisão 88). **SEO preservado de propósito:** `<title>`, meta description e JSON-LD continuam com "orçamento/preço de impressão 3D", que é o que as pessoas pesquisam; mudou o h1, o lead e a ordem das seções, não as palavras-chave. **Prints novos gerados pelo próprio app**, renderizado sem janela (`ImageComposeScene`) com dados de exemplo fictícios (e-mail em `example.com`), em vez de captura de tela manual; o PDF de exemplo reaproveita a foto real do porta-celular que já estava no print anterior. **Achado da auditoria que pesa mais que o texto:** a última release publicada é a v1.11.3, então o botão de download do site entrega uma versão sem nada do que foi feito depois; registrado no roadmap como pendência, porque publicar uma release (criar a tag e publicar o rascunho) é ação que o responsável do projeto confirma (decisão 33) | Aprovada |
| 91 | 2026-09-23 | **Revisão da decisão 88, antes de qualquer publicação:** a assinatura "Gerado com 3DReport" passa a ser **obrigatória**, sem opção de desligar. Motivo, do responsável do projeto: se der pra desligar, todo mundo desliga, porque ela não é essencial pra quem vende, e ela é a única divulgação de um app gratuito e sem verba (decisão 15). O compromisso passa a ser a discrição, já que o objetivo principal do app são as vendas do vendedor: 7 pt em cinza claro no canto inferior direito do PDF, longe do nome, da logo e do preço. **Só a palavra "3DReport" é link** pro site (antes era a frase inteira), pra o clique cair no nome do app. **Agora também na imagem quadrada do WhatsApp**, pequena (22 px) e apagada no canto inferior direito da faixa escura, sem tocar a foto e sem cruzar o prazo, que fica à esquerda; a faixa ganhou 8 px embaixo pra isso. Na imagem não há link (PNG não tem), então o texto é o próprio caminho: "3DReport" é o que alguém pesquisa depois de ver a imagem no status de um vendedor. Continua fora do texto do copiar/colar e do WhatsApp. **Sai o aviso único** da decisão 88: sem nada a escolher, ele só interromperia o trabalho; o FAQ do site e o CHANGELOG explicam. Com ele sai o `NoticesRepository`, que ficou sem uso. `BrandingSettings.showAppSignature` e a checkbox de Configurações saem; JSON salvo com o campo continua carregando (`ignoreUnknownKeys`). `PdfLayoutOptions.showAppSignature` fica, como chave do renderizador (padrão desligado pra testes e uso interno); os exports do app sempre resolvem ligado. **De passagem:** "Salvar imagem pro WhatsApp", no menu Ações, vira **"Salvar orçamento pro WhatsApp"**, pra ninguém achar que salva só a foto: é o orçamento inteiro em imagem | Aprovada |
| 92 | 2026-09-24 | Leva 2 do plano de evolução, item em aberto (serviço por pedido), junto com o preço do serviço por orçamento pedido pelo usuário. **O valor do serviço é do pedido, não do catálogo:** pintura, lixamento e acabamento variam com tamanho e detalhe de cada peça, então um preço fixo quase nunca era o valor certo. `Service.price` virou opcional e passou a ser só um **valor sugerido**, que preenche o campo quando o serviço é marcado e pode ser editado ali. Remover o preço do cadastro de vez foi descartado: quem cobra sempre o mesmo (embalagem, por exemplo) teria que redigitar em todo orçamento. **Por peça ou por pedido** (`chargedPerOrder`) tem um padrão no cadastro e pode ser trocado no orçamento; o seletor só aparece com quantidade maior que 1, porque com uma peça a distinção não muda nada e só poluiria a tela. Ter o marcador só no cadastro obrigaria a cadastrar "Pintura (lote)" à parte; só no orçamento, entrega começaria sempre errada. **O que foi cobrado vira `QuoteService`** (id, nome, valor, forma de cobrança), separado do `Service` do catálogo, porque um tem valor opcional e o outro não. Ele tem o mesmo formato JSON que o `Service` antigo gravado no histórico, então orçamentos salvos abrem sem migração e caem em "por peça", que é como foram calculados; `services.json` antigo também abre igual, com o preço virando sugestão. `QuoteService.total(quantity)` é a única conta de serviço, usada pelo total do `SavedQuote`, pela tela, pelo PDF e pelo copiar/colar, então Dashboard e exports não divergem. **Serviço marcado sem valor bloqueia o salvar** em vez de contar como zero, pelo mesmo raciocínio da decisão 77: entre dois enganos, fica o que aparece. **Reabrir usa o retrato salvo, não o catálogo:** antes, editar e salvar de novo reprecificava os serviços pelo catálogo atual sem avisar; agora valor e forma de cobrança vêm do orçamento, e um serviço excluído do catálogo continua marcado (com o nome salvo) pra não sumir do pedido. Serviços continuam fora do lucro (decisão 25) | Aprovada |
| 93 | 2026-09-24 | **Acabamento e mão de obra passam a ser independentes, e os dois só somam** (substitui o item (2) da decisão 76). Relato de usuário: com a hora em zero, anotar o preço; configurar R$ 50/h; o preço caía. Causa: a hora > 0 desligava sozinha a taxa de acabamento (10% do material) e passava a cobrar acabamento pelos minutos de trabalho de cada orçamento, mas esse campo só aparecia nesse momento e começava vazio. O acabamento saía, nada entrava no lugar, e a reserva de falha e a margem caíam junto. O cálculo estava certo; a regra amarrava duas coisas que ninguém percebe como ligadas. **Agora** `acabamento = material × finishingRate` sempre, e `laborRatePerHour` só soma: mexer em qualquer campo de Configurações nunca baixa o preço. Quem cobra lixar e pintar em minutos deixa a taxa em 0, e é o texto das duas telas que diz isso, não uma regra escondida. **Migração única sem mudar preço:** `PricingSettings.schemaVersion` (arquivo sem o campo = 1) e `migrated()`, aplicado ao ler o settings.json (o que também cobre backup restaurado): versão 1 com hora > 0 tem `finishingRate` zerado uma vez, que é o que o cálculo antigo já fazia, e o arquivo é regravado na versão 2; daí em diante uma taxa digitada de propósito nunca é mexida. **Descartadas:** (a) um seletor "Percentual do material / Pelo meu tempo de trabalho" (chegou a ser implementado e não foi lançado): resolvia o bug, mas exigia entender uma regra e duplicava a decisão que a própria taxa já expressa ao ser zerada; (b) uma pergunta "Como você cobra seu trabalho?" com um campo por vez: é a regra antiga à vista, não permite combinar percentual com hora e ainda deixaria o preço cair ao escolher "por hora" com os minutos vazios | Aprovada |
| 94 | 2026-09-24 | **Tempo de trabalho num campo só, do pedido inteiro** (revisa a parte de preparo da decisão 77). Havia dois campos: "Seu tempo de trabalho por peça" (multiplicado pela quantidade) e "Preparo do pedido" (uma vez). Com 1 peça eram a mesma coisa, e com várias a pessoa tinha que separar de cabeça uma conta que ela faz como um número só ("esse pedido me dá 1h30"). Agora é **um campo, "Seu tempo de trabalho no pedido (min)"**, depois da quantidade, sem multiplicar. **Custo aceito:** mudar a quantidade não refaz o tempo sozinho; o texto do campo mostra "Total das N peças" e "Mudou a quantidade? Revise o tempo." **Sem migração e sem quebrar o histórico:** o app grava o total em `Quote.setupMinutes` (já é "cobrado uma vez pelo pedido" no motor) e deixa `PrintJob.laborMinutes` em zero; o nome do campo ficou pra não mudar o JSON salvo. `Quote.totalLaborMinutes` (peça × quantidade + pedido) é o que a tela mostra ao reabrir, então orçamentos antigos abrem com o total somado e o preço igual. O `core` continua aceitando os dois, pra quem o usa como biblioteca. Junto: lembrete (não erro) quando há hora configurada e o tempo está vazio, "Seu trabalho ainda não entra no preço". **Descartadas:** dividir em "por peça" e "uma vez no pedido" só com quantidade > 1 (a divisão voltaria no caso mais difícil de explicar) e manter os dois campos | Aprovada |
| 95 | 2026-09-24 | Leva 6 do plano de evolução, parte do Dashboard: **lucro por hora, ranking de peças e ranking de clientes com desconto**. **Venda é o que o cliente fechou:** a análise mostrou que o Dashboard somava todo orçamento salvo, inclusive os que ficaram em "Orçado", então "Total vendido" e "Lucro" já contavam dinheiro que nunca entrou, e um "sua máquina te paga R$ X/h" em cima disso seria enganoso. `OrderStatus.isSold` (tudo menos `ORCADO`) é a única regra, e `QuoteReport` separa vendidos de abertos; o Dashboard inteiro passa a somar só vendidos, e os abertos aparecem numa linha à parte com o valor e a taxa de conversão. Isso muda o número que o Dashboard mostrava, e o CHANGELOG avisa. Manter as somas antigas e usar vendidos só nas métricas novas foi descartado: a mesma tela teria duas bases. **Lucro por hora de máquina** = lucro ÷ horas de impressão (tempo de uma peça × quantidade, a mesma conta da fila). **Hora de trabalho** = (lucro + mão de obra) ÷ seu tempo, só nos pedidos com tempo de trabalho informado (lucro e mão de obra também só desses, pra razão não misturar bases), mostrada ao lado da hora configurada: a mão de obra já está no custo, e sem somá-la de volta o número seria só o lucro acima do que você se pagou, difícil de ler. Lucro ÷ horas de trabalho foi descartado por isso. **Ranking de peças:** top 5 por lucro, com lucro/h de máquina por linha (é o que diz qual peça vale repetir); pedidos de mesmo nome (sem diferenciar maiúsculas e espaços) são a mesma peça, o que junta as reimpressões duplicadas do Histórico. O nome automático ("Orçamento - dd/MM/aaaa HH:mm") fica de fora, e o prefixo foi pro `core` (`SavedQuote.AUTO_NAME_PREFIX`/`hasAutoName`) pra quem salva e quem agrega usarem a mesma regra. **Ranking de clientes que puxam o preço pra baixo** (sobra da decisão 84): soma de `negotiatedDiscount` por cliente, só quem ficou com desconto líquido positivo, uso interno. **Limitação aceita:** o período continua recortando pela data de criação do orçamento, porque não existe data de venda no modelo | Aprovada |
| 96 | 2026-09-24 | Leva 6 do plano de evolução, parte da **manutenção por impressora** (decisão 83): alertas por componente, diário e horas fora de orçamento, **sem aba nova**. Botão "Manutenção" em cada impressora da aba Impressoras abre um diálogo com três blocos: componentes, diário e horas avulsas; a linha da impressora mostra o componente mais urgente (vermelho vencido, âmbar a 10% do intervalo ou menos, discreto em dia). **Horas da máquina** = tempo dos pedidos **Prontos ou Entregues** naquela impressora (uma peça × quantidade, a mesma conta da fila) + horas avulsas; "Em impressão" fica de fora porque ainda não terminou. **Contador por fotografia, não por data:** o modelo não sabe quando cada pedido foi impresso, e gravar essa data mexeria no formato do histórico sem cobrir os pedidos antigos. Então fazer a manutenção guarda o total de horas daquele momento em `MaintenanceComponent.hoursAtLastService`, e o contador é total atual menos isso. Ao cadastrar um componente, "horas desde a última vez" (opcional) cobre a máquina que já rodava antes do app, e a fotografia pode ficar negativa por isso. Registrar no diário escolhendo um componente zera o contador dele **agora**, mesmo com data antiga, e a tela diz isso. **Limitação aceita:** voltar o status de um pedido ou excluí-lo diminui o total; o contador nunca fica negativo (`max(0, …)`) (efeito real descrito na decisão 97). **Arquivo próprio** (`maintenance.json`, `PrinterMaintenance`), sem mexer em `PrinterProfile` nem no `printers.json`; o backup já leva a pasta inteira. Excluir a impressora apaga a manutenção dela (o aviso de exclusão diz). **Sem intervalo sugerido:** o cadastro oferece nomes comuns (Bico, Lubrificar eixos, Correias, Limpeza da mesa), mas o número varia por máquina e o app não inventa, mesma disciplina das decisões 55 e 56. `MaintenanceReport` é função pura no `core`, no estilo do `PrintQueueReport` | Aprovada |
| 97 | 2026-09-24 | Correções da revisão de código da decisão 96. **(1) Zerar um contador passa a ter desfazer.** "Feito hoje" grava no diário e zera o componente sem confirmação, e excluir a entrada não devolvia as horas: um clique errado com o bico em 45 de 50 h fazia o próximo aviso chegar ~45 h atrasado, sem jeito de consertar pela tela. Agora `MaintenanceLogEntry.previousHoursAtLastService` guarda onde o contador estava, e excluir a manutenção **mais recente** de um componente (pela ordem de registro, não pela data, que pode ser antiga) devolve o contador a esse valor; excluir uma mais antiga só tira do diário, porque o contador atual já veio de uma posterior. Vale também pra "Registrar manutenção" com o componente errado. A regra fica em `PrinterMaintenance.withService`/`withoutLogEntry`, no `core`, e o diálogo diz que dá pra desfazer. Pedir confirmação a cada "Feito hoje" foi descartado: é ação frequente, e o desfazer resolve o engano sem somar clique. Entradas gravadas na 1.41.0 não têm o valor anterior e só saem do diário. **(2) Correção do texto da limitação da decisão 96.** "O contador nunca fica negativo" escondia o efeito: se, depois de uma manutenção feita com a máquina em 100 h, sai do total um pedido de 30 h impresso antes dela (excluído ou com o status voltado), o total cai pra 70 h e as próximas 30 h de impressão não contam pro componente, então o aviso chega 30 h atrasado. Continua aceito: o conserto de verdade é gravar quando cada pedido terminou de imprimir, o desenho por data descartado na 96, e o caso exige mexer num pedido anterior à manutenção | Aprovada |
| 98 | 2026-09-24 | Leva 9 do plano de evolução, item **calculadora web no próprio site, reaproveitando o `core`**. **Kotlin/JS com formulário HTML, não Compose para Web (Wasm):** o Compose no navegador baixa vários MB e desenha num canvas, que o Google não lê e celular fraco demora pra abrir; o objetivo do item é justamente SEO. O `core` ganhou o alvo `js { nodejs() }` ao lado do `jvm()`, e **os testes de `commonTest` passaram a rodar nos dois alvos**, o que garante que a conta do navegador é a do app. Compilar pra JS achou três pontos só da JVM no `commonMain`, corrigidos sem mudar comportamento: `HashMap.putIfAbsent` no `StlAnalyzer` (virou `getOrPut`) e flags de regex embutidas (`(?im)`, `(?is)`, `(?i)`) no `GCodeMetadataParser` e no `StlParser`, que o JavaScript não aceita (viraram `RegexOption`; o `.` com `(?s)` virou `[\s\S]`); `\[mm]` ganhou o `]` escapado, exigido no modo Unicode do JS. **Módulo novo `:web`** (`js { browser() }`, bundle UMD de ~75 KB, objeto global `web`): `WebCalculator.calculateQuote` é **só fachada**, recebe números (peso em gramas, percentuais de 0 a 100), monta os modelos do `core` e chama `PricingCalculator.calculate`; gramas viram metros com `Filament.weightGrams(1.0)`, o inverso da própria fórmula (a densidade não muda o custo com a entrada em gramas); o tempo de trabalho é do pedido inteiro, como na decisão 94; erro de validação do `core` (ex.: canal + imposto chegando a 100%) volta como mensagem, não como exceção. A página (`site/assets/calculadora.js`) não faz conta nenhuma além de juntar horas e minutos. **Página própria** (`site/calculadora.html`, título e URL voltados a "calculadora preço impressão 3D"), ligada pela nav, pelo hero e por "Como o cálculo funciona", e no `sitemap.xml`; na home ela disputaria a mensagem de "sistema do vendedor" da decisão 90. **Recorte dos campos pra não canibalizar o app:** visíveis peso, tempo, R$/kg, potência, kWh e margem; recolhidos em "Mais custos" falhas, acabamento, hora de trabalho e minutos, quantidade, taxa do canal e imposto. Ficam só no app: catálogos, manutenção e retorno da máquina, custo fixo mensal, administrativo, serviços, G-code, histórico, clientes e PDF, e a chamada abaixo do resultado diz isso. Resultado com valor de venda (total e por peça), produção, lucro, ponto de equilíbrio e detalhamento, porque o site vende "fórmula sem caixa-preta". Campos lembrados no navegador (`localStorage`). **Publicação:** o bundle é gerado, não versionado (`site/assets/calc/` no `.gitignore`); `pages.yml` instala o JDK, roda `./gradlew :web:syncSiteCalculator` e passa a disparar também com mudanças em `core/`, `web/` e no Gradle, pra uma mudança de fórmula republicar a calculadora sozinha. O app desktop não muda, então não há versão nova | Aprovada |
| 99 | 2026-09-24 | **Menu do site no celular vira um botão "Menu"** (levantado ao testar a calculadora da decisão 98 em 375 px). Abaixo de 860 px, os links do cabeçalho quebravam em várias linhas e, com o cabeçalho fixo (`position: sticky`), ocupavam uns 230 px (275 px com o link da calculadora) o tempo todo, cerca de um terço da tela. **Escolha do responsável do projeto entre três opções** (botão que abre, faixa que rola de lado, menos links no celular): botão com ícone e texto ("☰ Menu", regra da decisão 87: ícone só ao lado de texto) à direita do logo; ao tocar, os links abrem numa coluna embaixo do cabeçalho; fecha ao tocar num link, no botão de novo ou com Esc. `aria-expanded`/`aria-controls` no botão. **Sem JS nada quebra:** o `site/assets/nav.js` marca o cabeçalho com `.nav-ready`, e só com essa classe o CSS esconde os links; sem o script, eles aparecem como antes. Vale pra `index.html`, `manual.html` e `calculadora.html` (o `404.html` não tem menu). No computador, nada muda. Cabeçalho fechado no celular: 73 px | Aprovada |
| 100 | 2026-09-24 | **Calculadora do site deixa claro que é uma parte do app, e a página ganha acabamento** (pedido do responsável do projeto depois de ver a decisão 98 publicada). **O que falta fica à vista no resultado:** a legenda vira "Preço de venda parcial" e o detalhamento termina com três linhas apagadas, "Manutenção da impressora", "Retorno da máquina" e "Custo fixo do mês", com a etiqueta "só no app" no lugar do valor, mais a nota "Esses custos só aumentam o preço". **Tabela "calculadora x app"** logo abaixo da calculadora, substituindo a caixa azul: só recursos que existem no app (preço completo, G-code, catálogo com estoque, histórico e clientes, PDF e WhatsApp, status dos pedidos e Dashboard) e "Grátis" nas duas colunas, com o botão de download no fim. **Acabamento visual:** campos em grade de duas colunas (uma só abaixo de 480 px), tempo com "h" e "min" dentro dos campos, o preço mínimo vira uma linha normal em vez da faixa âmbar que parecia alerta, menos espaço entre o título e o formulário. **Correções de site inteiro achadas na revisão:** (1) o botão principal tinha texto branco sobre o azul claro do modo escuro, ilegível também na home; ganhou o token `--on-primary`. (2) O GitHub Pages serve CSS e JS com 10 minutos de cache, e logo depois do deploy da decisão 98 a página nova abriu com o CSS velho, sem estilo nenhum. Os HTMLs passam a apontar pra `?v=dev` e o `pages.yml` troca por um trecho do commit publicado, então cada deploy muda o link | Aprovada |
| 101 | 2026-09-24 | Leva 7B do plano de evolução, **Fase 1: produto separado de pedido** (levantado pelo responsável do projeto; revisita a decisão 44 com um argumento novo, o público de quem ainda não vende). **Campo, não modelo novo:** `SavedQuote.kind` (`QuoteKind.ORDER`/`PRODUCT`), com padrão Pedido, reaproveita foto, STL, configurações de impressão, PDF, imagem, WhatsApp, edição e comparar impressoras sem duplicar código. Pedido não grava o campo (valor padrão), então o `quotes.json` de quem nunca cria produto continua igual byte a byte. **Uma regra só:** `SavedQuote.isOrder`, no espírito do `isSold` (decisão 95); `QuoteReport`, `PrintQueueReport` (aba Impressoras e dica de prazo) e `MaintenanceReport` filtram por ela no `core`, e `isDeliveryOverdue` é sempre falso pra produto. Produto grava cliente, frete e prazo vazios, garantido no repositório (e não só na tela) e mantido na edição; o `status` dele fica parado em Orçado e é ignorado. **Na tela de Orçamento, seletor "Pedido de cliente \| Produto do catálogo" no topo da seção de salvar + um botão só, cujo texto muda** ("Salvar como pedido"/"Salvar no catálogo"), em vez dos dois botões previstos na roadmap: esconder cliente, prazo e frete exige saber o tipo antes de preencher, e o frete fica lá em cima, nos dados da peça; com dois botões no fim, ou os campos ficariam visíveis e seriam descartados, ou haveria seletor e botões redundantes (escolha do responsável entre as três). O tipo fica em `QuoteInputState` porque muda o cálculo (produto ignora frete e preço fechado com cliente, e a seção "Negociação" vira "Preço mínimo") e continua escolhido depois de salvar. O seletor some na edição e quando o formulário veio de "Vender" ou "Guardar no catálogo". **Histórico:** seletor `Pedidos \| Produtos` ao lado de Lista/Kanban, sem aba nova (barra e atalhos como a decisão 82 deixou); em Produtos não há Kanban nem filtro de status, a busca é só por nome, e trocar de lista esvazia a seleção. Card do produto com **"Vender"** como botão principal (ícone `sell` do Material Symbols), que abre o Orçamento pelo mesmo caminho do Duplicar, em modo pedido, sem cliente nem prazo; o pedido grava **`sourceProductId`**, antecipado da Fase 3 porque o "Transformar em pedido" precisa saber se o produto já foi vendido. **"Guardar no catálogo"** (lista e Kanban) passa pelo Orçamento em modo produto em vez de copiar direto: preço negociado com aquele cliente volta ao de tabela e a pessoa revisa; o pedido não muda. **"Transformar em pedido"** muda o próprio registro, que nasce Orçado com a data da conversão (achado da revisão de código: com a data antiga do produto, o pedido sumia dos filtros de período do Histórico e do Dashboard e ia pro fim da lista), e só aparece pra produto nunca vendido. **"Exportar catálogo"** em Produtos leva todos os produtos visíveis sem seleção, ou só os marcados. **Limitação aceita:** uma versão anterior do app ignora o campo (`ignoreUnknownKeys`) e mostra produtos como pedidos Orçados, sem perda de dado. Fases 2 e 3 continuam na roadmap | Aprovada |
| 102 | 2026-09-24 | Leva 7B, **Fase 2: catálogo vivo**, mais dois ajustes pedidos pelo responsável do projeto depois de testar a Fase 1 (decisão 101). **Atualizar preço:** `ProductRepricer` no `core` (função pura, testada em JVM e JS) acha filamento, impressora e canal pelo que o produto guardou (id, `printerId` e nome do canal, como a reabertura do Orçamento já fazia) e refaz a conta com as mesmas medidas, quantidade, tempo de trabalho e cor; faltando algum cadastro, ou com orçamento antigo sem `printerId`, devolve o motivo em vez de adivinhar. **O aviso é por custo, não por idade** (escolha do responsável, revisando a roadmap): o card do produto recalcula ao aparecer e só mostra "Custos mudaram: hoje o preço calculado seria R$ X" + "Atualizar preço" quando o calculado mudou mais de meio centavo, porque preço antigo com custos iguais continua certo. O diálogo mostra antes e depois de preço calculado, produção e lucro, e avisa em vermelho se o anunciado passou a dar prejuízo. Gravar troca só o `Quote` (`QuoteHistoryRepository.updateQuote`) e marca a edição, que vira a data do preço; pedido nunca passa por isso (retrato congelado). O `QuoteHistoryViewModel` passa a receber os repositórios de filamentos, impressoras, configurações e canais, e a tela coleta os quatro pra o aviso reagir na hora a uma mudança de cadastro. **Preço anunciado reaproveita o preço fechado da decisão 84:** em produto, o campo que a Fase 1 tinha escondido volta como "Preço anunciado no catálogo" (anunciado em `salePrice`, calculado em `tableSalePrice`), com o atalho "Arredondar pra R$ X,90" (próximo valor terminado em ,90) e o aviso de prejuízo da decisão 79; o recálculo mantém o anunciado e só mexe no calculado. "Vender" leva o anunciado como preço fechado do pedido, porque o cliente paga o que viu. Sem anunciado, "Vender" abre o pedido recalculado com os custos de hoje, mesmo que o card ainda mostre o preço antigo: é o que condiz com a venda real, e o aviso "Custos mudaram" no card já deixa o vendedor avaliar antes (escolha do responsável, descartando preencher o preço antigo como preço fechado, que faria o Dashboard contar essas vendas como negociadas). **Categoria** (`SavedQuote.category`, texto livre, padrão `null`) só em produto: o repositório descarta em pedido, tira espaços e reaproveita a grafia de uma já usada ignorando maiúsculas; o campo sugere as já usadas; o Histórico ganha chips "Todas as categorias", cada uma e "Sem categoria" (quando há dos dois); o catálogo em PDF agrupa em seções alfabéticas com "Outros" no fim, em fluxo (página nova só quando o próximo bloco não cabe, e título de seção nunca sozinho no pé da página), e sem categorias sai igual ao de antes. **Barra de operação no rodapé do Orçamento:** em Vender, Duplicar e Guardar no catálogo, o aviso e o "Cancelar" saem do meio do formulário, onde eram difíceis de achar, pra uma faixa fixa embaixo com fundo `errorContainer` e botão contornado em vermelho ("Cancelar venda", "Cancelar cópia", "Cancelar duplicação"); cancelar limpa o formulário e volta ao Histórico, onde as três começam (escolha do responsável). A edição continua com o "Cancelar edição" fixo no topo do diálogo. **Hierarquia no Histórico:** segmentado `Pedidos (N) \| Produtos (N)` em cima e, só em Pedidos, "Exibir como: Lista · Kanban" menor e recuado embaixo, com ícones `view_list`/`view_kanban` (escolha do responsável entre três desenhos); na mesma linha, Lista e Kanban pareciam opções do mesmo nível. Ícones novos copiados do Material Symbols: `sync`, `close`, `info`, `view_list`, `view_kanban`. **Correções da revisão de código:** (1) "Vender" colocava o anunciado no preço fechado, que é o total do cliente com frete, e o frete da venda saía da peça (anunciado R$ 50 + frete R$ 15 dava peça de R$ 35); agora o anunciado por unidade fica em `QuoteInputState.announcedUnitPrice`, o campo de preço fechado nasce vazio com a frase "Preço anunciado do produto: R$ X. Frete e serviços somam por fora", e a peça sai pelo anunciado vezes a quantidade, a menos que se digite outro preço. (2) Trocar entre pedido e produto limpa o preço fechado, que em cada tipo significa uma coisa. (3) Editar um produto pode corrigir a maiúscula da própria categoria (a busca por grafia existente ignora o produto editado). (4) O atalho de arredondar só aparece com o campo de anunciado vazio | Aprovada |
| 103 | 2026-09-24 | Leva 7B, **Fase 3: quem está começando** (a leva fecha aqui), mais o redesenho da barra de operação da decisão 102. **Perfil de uso** (`UsageProfile`: "Já vendo sob encomenda" ou "Estou começando"), primeira pergunta do onboarding e seção "Como você usa o app" em Configurações. Só define **padrões**: o tipo que vem escolhido num orçamento novo (pedido ou produto) e a lista que o Histórico abre (Pedidos ou Produtos); nada some por perfil. Arquivo próprio (`usage-profile.json`), pelo mesmo motivo do onboarding: é estado da interface, não custo. Padrão "Já vendo", então quem já usava o app (e não verá o onboarding de novo) continua exatamente como antes. Trocar o perfil vale na hora pra lista do Histórico e pro orçamento **em branco**; um orçamento com algo digitado, ou uma operação/edição em andamento, fica como está (`QuoteViewModel.applyDefaultKindIfUntouched`). **Dashboard sem vendas mostra o catálogo:** `CatalogReport` no `core` (função pura, JVM e JS) resume os produtos, sem recorte de período porque catálogo é o que se oferece hoje: quantos são, faixa de preço por unidade e a lista "Qual vale a pena oferecer primeiro", ordenada por lucro por hora de máquina (sem tempo de impressão, no fim), com o convite "Quando aparecer um cliente, é só clicar em Vender". Aparece **só quando não há vendas no período** (escolha do responsável); quem vende continua vendo só as vendas. Sem vendas e sem produtos, a mensagem vazia convida a montar o catálogo. **Ranking pelo produto de origem:** `QuoteReport` junta as vendas pelo `sourceProductId` (antecipado na decisão 101), então renomear o produto não parte o ranking; pedido sem origem com o nome de um vendido pelo catálogo entra junto, e o resto continua pelo nome. "Duplicar" passa a levar a origem do pedido duplicado. **Barra de operação mais discreta** (pedido do responsável, "chama muita atenção"; escolha entre três desenhos): o vermelho (`errorContainer` + botão vermelho) dizia erro ou ação destrutiva no Material 3, e cancelar ali não apaga nada. Agora é neutra: linha fina em cima, fundo `surfaceVariant` a 35%, filete de 4 dp e ícone em `secondary` (âmbar), a mesma assinatura das faixas de destaque do PDF (decisão 35), título curto em `titleSmall` com a explicação em cinza embaixo, e botão contornado padrão (azul). Continua fixa e com o mesmo comportamento. **De quebra:** o app não define `tertiary`, e o aviso "Custos mudaram" (decisão 102) usava essa cor, que caía no roxo padrão do M3; passou pro âmbar da paleta. **Correções da revisão de código:** (1) o pedido do "Vender" guardava o anunciado como preço fechado e contava como negociação: quem comprava pelo preço do catálogo entrava no ranking de desconto do Dashboard, com desconto negativo quando o anunciado era arredondado pra cima. Agora `SavedQuote.soldAtCatalogPrice` marca o pedido que saiu pelo anunciado sem outro preço digitado; `isNegotiatedWithClient` e `clientDiscount` deixam esses pedidos fora da contagem, do total e do ranking de descontos; o Histórico mostra "Preço do catálogo (calculado R$ X)"; editar reabre com o anunciado no lugar dele, sem virar preço digitado, e digitar outro valor passa a ser negociação de verdade. (2) O resumo do catálogo no Dashboard mostrava o preço de uma unidade sem serviços, e o catálogo em PDF mostra o total com serviços e quantidade; o Dashboard passou a usar o mesmo preço do PDF (`totalWithServices`) e o lucro do produto inteiro | Aprovada |
| 104 | 2026-09-25 | Leva 9, **formato de dados 2** (vale a partir da 2.0.0). O app ainda não está em produção e o responsável do projeto autorizou quebrar o formato do JSON onde isso trouxer benefício claro; ele escolheu **recomeçar do zero, sem conversor**. O modelo do orçamento passou a ser em lista (ver decisão 105) e o legado de compatibilidade saiu na mesma quebra: `Quote.marketplaceFeeRate` virou `channelFeeRate`; `Quote.setupMinutes`, `PrintJob.laborMinutes` e `Quote.totalLaborMinutes` viraram um campo só, `Quote.laborMinutes` (o trabalho é do pedido, decisão 94); `Quote.productionCost` e `Quote.filamentWeightGrams` deixaram de ser gravados e são calculados (dado duplicado podia divergir); `printerId`/`printerName` saíram do `Quote` pra cada impressão (`QuotedPrint`) e são obrigatórios, o que tirou os casos "orçamento sem impressora" da fila, da manutenção e do repricer; o canal passou a ser guardado por `channelId` + `channelName`, e o `ProductRepricer` acha pelo id (renomear o canal não deixa mais o produto sem canal); `PricingSettings.marketplaceFeeRate`, `schemaVersion` e `migrated()` saíram, junto da migração pra canal em `SalesChannelRepository` e do repasse em `SettingsViewModel`; `BrandingSettings.watermarkText`/`QuoteTemplate.watermarkText` viraram `brandName`; `Service.price` virou `suggestedPrice`; `QuoteService.chargedPerOrder` ficou obrigatório; `CostBreakdown.labor`/`fixedCost` perderam o valor padrão. `ignoreUnknownKeys` continua ligado: protege abrir um arquivo mais novo numa versão mais velha, não é legado. **Pasta de dados:** `~/.3dreport/format.json` guarda a versão (`DATA_FORMAT_VERSION = 2`), conferida no `Main.kt` antes de qualquer repositório ler (`prepareDataDir`). Pasta com dados e sem esse arquivo (ou com versão menor) é **movida inteira, sem apagar nada**, pra `~/.3dreport-v1` (`-v1-2`, `-v1-3` se já existir), e o app mostra **um aviso uma vez**, antes do onboarding, com o caminho e como recuperar (reinstalar a 1.44 e renomear a pasta de volta). Pasta de uma versão **mais nova** (quem voltou pra esta) também é guardada à parte, em `~/.3dreport-v3`, com um aviso que manda atualizar o app, porque abrir aqueles arquivos pela metade e gravar por cima perderia dados. Um `format.json` que existe mas não abre (gravação interrompida) conta como a versão atual e é regravado: só esta versão grava esse arquivo, e mover os dados por causa dele seria pior. Mover a pasta tenta renomear e, se falhar (no Windows, pasta aberta em outro programa), copia tudo e só então esvazia a original; se nem copiar der, o app **não abre** e explica o que fazer, porque ler os arquivos 1.x com os campos renomeados vazios e gravar por cima apagaria nome da marca e preços sugeridos. **`readJsonFile` para de perder dado em silêncio:** antes, um arquivo ilegível virava o valor padrão e a próxima gravação o sobrescrevia; agora, quando o conteúdo não bate com o formato, ele é guardado ao lado como `<nome>.ilegivel-<millis>.json` (falha de leitura do disco não renomeia nada). **Revisão de código (mesmo dia):** o Ctrl+S calculava com os filamentos esgotados e podia salvar outro preço que o da tela (agora usa a mesma lista); importar um G-code com vários extrusores e sem o consumo de cada um não junta mais as linhas que a pessoa separou à mão; vários slots do mesmo filamento sem consumo por slot ainda escolhem aquele filamento; reabrir, duplicar ou vender um orçamento cujo canal foi excluído e recriado acha o canal pelo nome, e sem canal nenhum a tela avisa que o preço está sem a taxa, em vez de recalcular sem ela em silêncio. **Backup:** o manifesto ganhou `dataFormatVersion`, e restaurar backup de outro formato é recusado com mensagem, sem mexer nos dados atuais. **Versão:** primeiro MAJOR do projeto (2.0.0), porque o `development.md` define quebra de compatibilidade como MAJOR. | Aprovada |
| 105 | 2026-09-25 | Leva 9, **cálculo por impressão e vários filamentos numa impressão** (Fases 1 e 2 e o núcleo da 4 do roadmap). **Modelo:** `FilamentUsage` (filamento, metros, cor); `PrintJob` = uma mesa, com `filaments` (nunca vazia), tempo, `runs` (quantas vezes a mesma mesa roda) e nome opcional, mais um construtor de atalho pra um filamento só; `QuotedPrint` = retrato de uma impressão no orçamento (o `PrintJob`, a impressora e o `PrintCost` dela, congelado); `Quote.prints` (nunca vazia). A entrada (`PrintJob`, o que o fatiador diz) ficou separada do resultado (`QuotedPrint`, em qual máquina e quanto custou), do mesmo jeito que o `printerId` já vinha do cálculo e não da peça. **Cálculo:** `PricingCalculator.calculate(prints: List<Pair<PrintJob, PrinterProfile>>, …)`; material somado filamento por filamento; energia, manutenção, retorno e custo fixo com as horas e a impressora **daquela** impressão; acabamento sobre o material de cada impressão; tudo vezes `runs` e quantidade; só depois entra o que é do pedido, uma vez só (trabalho, administrativo, falha sobre o reimprimível, margem, canal, imposto, negociação). O atalho de uma impressão continua (`calculate(job, printer, …)`), e a calculadora do site usa ele. Os valores da planilha de referência não mudaram (os testes do `core` passam nos alvos JVM e JS com os mesmos números). **Relatórios:** fila e horas de manutenção somam em cada impressora só o tempo das impressões dela (`Quote.printMinutesOn`); o filamento mais usado do Dashboard conta **pedidos** que usaram cada filamento, sem repetir por impressão. **G-code por extrusor:** o parser devolve o consumo de cada extrusor (`GCodeFilament.lengthMeters`), casado pela posição com `filament_type`; se os tamanhos não batem, o consumo por extrusor fica de fora (não adivinha). Slot declarado e sem uso (`0.00`) é descartado, o que também corrige um aviso de multimaterial falso. `CatalogMatcher.matchFilaments` casa cada extrusor sozinho, pela regra de precisão da decisão 89, e `FilamentMatch.Multimaterial` saiu. **Evidência de purga:** o roadmap exigia conferir em G-codes multicolor reais se o valor por extrusor inclui a purga e a torre. Foram achados quatro arquivos públicos (Bambu Studio 1.10 com AMS lite; OrcaSlicer 2.2 IDEX com torre; OrcaSlicer 2.3 com slot sem uso; OrcaSlicer 2.4.2 com duas cores). No arquivo completo do OrcaSlicer IDEX, a extrusão somada de cada ferramenta (1384,7 e 1804,6 mm) bate com o cabeçalho (1384,73 e 1805,62 mm): **o valor por extrusor já inclui a torre**. O arquivo do Bambu Studio é recortado, então ali só o formato está conferido, e a inclusão da purga fica apoiada em o OrcaSlicer ser um fork do Bambu Studio com o mesmo processador de G-code. Trechos dos cabeçalhos viraram fixtures (`RealGCodeFixtures`), com origem e commit. **Tela (uma impressão só, na 2.0):** o estado do Orçamento já é em lista (`QuoteInputState.prints`, `PrintInput`, `FilamentInput`), pra a Fase 3 não refazer tudo. "+ Adicionar filamento" abaixo do filamento; com mais de uma linha, cada uma tem número, metros e remover, e o comprimento sai do campo único. Com uma linha só, a tela é a de antes. Numa peça multicolor, linha sem filamento escolhido não cai no primeiro do catálogo (cobraria o material errado sem ninguém ver); com uma linha só, o padrão de sempre continua. Importar G-code com consumo por extrusor cria uma linha por extrusor; o que não casou fica em branco pra escolher, e a mensagem lista cada extrusor. "Desfazer" devolve a impressão exatamente como estava antes da primeira importação, inclusive as linhas. A nota mostra o consumo por filamento, e o card do Histórico troca "Cor" por "Filamentos: PLA · Preto 42 g, PETG 80 g". PDF, texto, WhatsApp e imagem não mudam: o cliente nunca vê filamento (decisão 19). "Comparar impressoras" coloca todas as impressões na mesma máquina. | Aprovada |

## Pendentes de aprovação

Nenhuma no momento — ver [roadmap.md](roadmap.md) para as próximas evoluções.
